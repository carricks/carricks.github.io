---
import { projects } from "../../data/projects";
import BaseLayout from "../../layouts/BaseLayout.astro";

export function getStaticPaths() {
  return projects.map((project) => ({
    params: { slug: project.slug },
    props: { project },
  }));
}

const { project } = Astro.props;
const isResearchCaseStudy = project.slug === "brain-tumour-mri-classification";
---

<BaseLayout title={`${project.title} | Diego Carrillo`} description={project.summary}>
  {isResearchCaseStudy ? (
    <main class="case-study">
      <section class="case-hero">
        <nav aria-label="Project navigation" class="project-navigation">
          <a href="/#projects" class="interactive-control project-icon-control" aria-label="Back to projects" title="Back to projects">
            <svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round" stroke-linejoin="round">
              <path d="M20 12H4" /><path d="m10 6-6 6 6 6" />
            </svg>
          </a>
        </nav>

        <div class="case-hero-grid">
          <div class="case-hero-copy">
            <p class="case-eyebrow">MASTER'S RESEARCH · COMPUTER VISION</p>
            <h1>Classifying brain tumours from MRI.</h1>
            <p class="case-lead">An end-to-end research prototype that moved from heterogeneous medical images to a four-class convolutional model and a usable web interface.</p>
          </div>

          <figure class="hero-visual">
            <div class="scan-frame">
              <button type="button" class="figure-zoom-trigger" data-lightbox-trigger aria-label="Enlarge preprocessed MRI samples">
                <img src="/images/projects/brain-tumour-mri/mri-preprocessing.png" alt="Preprocessed MRI samples showing tumour and non-tumour brain images" />
                <span aria-hidden="true">Expand</span>
              </button>
              <div class="scan-line" aria-hidden="true"></div>
            </div>
            <figcaption>Preprocessed MRI samples from the experimental workflow.</figcaption>
          </figure>
        </div>
      </section>

      <section class="case-section impact-section" aria-labelledby="impact-title">
        <div class="section-heading">
          <p class="case-eyebrow">WHY IT MATTERS</p>
          <h2 id="impact-title">A difficult visual problem with real clinical consequences.</h2>
          <p>Brain and central nervous system tumours are uncommon compared with many cancers, yet their location and heterogeneity make assessment especially demanding. The project explored how computer vision could support image review without replacing specialist judgement.</p>
        </div>
        <div class="metric-grid">
          <article><strong>321,731</strong><span>New brain and CNS cancer cases worldwide in 2022</span></article>
          <article><strong>248,500</strong><span>Estimated deaths worldwide in 2022</span></article>
          <article><strong>4 Classes</strong><span>Meningioma, glioma, pituitary and no tumour</span></article>
        </div>
        <p class="source-note">Global context: <a href="https://gco.iarc.who.int/media/globocan/factsheets/cancers/31-brain-central-nervous-system-fact-sheet.pdf" target="_blank" rel="noreferrer">IARC Global Cancer Observatory, GLOBOCAN 2022</a>. The prototype is research work and is not a validated medical diagnostic device.</p>
      </section>

      <section class="case-section" aria-labelledby="challenge-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">THE CHALLENGE</p>
            <h2 id="challenge-title">Teaching a model to distinguish visually similar patterns.</h2>
          </div>
          <div>
            <p>MRI datasets combine different resolutions, anatomical views and acquisition conditions. Tumours may also share location, shape and texture, making the boundary between classes difficult to learn.</p>
            <p>The work therefore focused as much on data quality and experimental discipline as on model architecture: combining public datasets, removing poor-quality samples, balancing classes and keeping test images outside augmentation.</p>
          </div>
        </div>
        <div class="class-grid" aria-label="Classification targets">
          {[
            { title: "MENINGIOMA", copy: "Tumour arising from the meninges surrounding the brain.", image: "class-meningioma.png" },
            { title: "GLIOMA", copy: "Tumour originating in glial cells, with highly variable appearance.", image: "class-glioma.png" },
            { title: "PITUITARY", copy: "Tumour located around the pituitary gland at the base of the brain.", image: "class-pituitary.png" },
            { title: "NO TUMOUR", copy: "MRI samples without evidence of the three target tumour classes.", image: "class-no-tumour.png" },
          ].map(({ title, copy, image }) => (
            <article>
              <button type="button" class="figure-zoom-trigger class-image-trigger" data-lightbox-trigger aria-label={`Enlarge MRI example classified as ${title.toLowerCase()}`}>
                <img src={`/images/projects/brain-tumour-mri/${image}`} alt={`MRI example classified as ${title.toLowerCase()}`} />
                <span aria-hidden="true">Expand</span>
              </button>
              <div><h3>{title}</h3><p>{copy}</p></div>
            </article>
          ))}
        </div>
      </section>

      <section class="case-section" aria-labelledby="pipeline-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">RESEARCH PIPELINE</p>
          <h2 id="pipeline-title">From raw images to an interpretable prediction.</h2>
        </div>
        <ol class="pipeline">
          <li><span>DATA</span><div><strong>Combine & curate</strong><p>Bring together three public MRI collections, remove low-resolution samples and reorganise the material into balanced classes suitable for controlled experimentation.</p></div></li>
          <li><span>INPUT</span><div><strong>Standardise</strong><p>Convert every image to a consistent 224 × 224 RGB input, then separate training, validation and test data so evaluation remains independent from learning.</p></div></li>
          <li><span>LEARN</span><div><strong>Train & augment</strong><p>Introduce controlled rotations, shifts, zoom and flips only during training, increasing visual diversity without modifying the validation or test evidence.</p></div></li>
          <li><span>MODEL</span><div><strong>Transfer knowledge</strong><p>Reuse VGG16's pretrained visual features, freeze the established representation and adapt the final layers to distinguish the four MRI classes.</p></div></li>
          <li><span>OUTPUT</span><div><strong>Evaluate & serve</strong><p>Compare class-level behaviour through a confusion matrix, then serve the resulting probabilities through Flask for numerical and D3.js visual presentation.</p></div></li>
        </ol>
      </section>

      <section class="case-section visual-story" aria-labelledby="model-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">MODEL EVOLUTION</p>
            <h2 id="model-title">The first model revealed the problem. VGG16 made it scalable.</h2>
          </div>
          <div>
            <p>An initial two-class CNN established the preprocessing and evaluation workflow, but its small dataset produced unstable validation behaviour. Increasing and cleaning the data improved the experiment before expanding it to four classes.</p>
            <p>Transfer learning reused VGG16's pretrained visual features. Freezing most of the network and adapting the output reduced the trainable portion to roughly 16,000 parameters for the target classification task.</p>
          </div>
        </div>
        <figure class="research-figure wide-figure">
          <button type="button" class="figure-zoom-trigger" data-lightbox-trigger aria-label="Enlarge VGG16 architecture diagram">
            <img src="/images/projects/brain-tumour-mri/vgg16-architecture.png" alt="VGG16 transfer-learning architecture used for four-class MRI classification" />
            <span aria-hidden="true">Expand</span>
          </button>
          <figcaption>VGG16 transfer-learning architecture adapted during the research.</figcaption>
        </figure>
        <div class="experiment-grid">
          <article><div><span>INITIAL EXPERIMENT</span><strong>Two-class CNN baseline</strong><p>A custom convolutional network used 620 images of meningioma and non-tumour cases to validate the complete workflow. Its unstable validation behaviour exposed overfitting and the limits of a small dataset.</p></div></article>
          <article><div><span>FINAL EXPERIMENT</span><strong>Four-class transfer learning</strong><p>The study expanded to 5,320 balanced images covering meningioma, glioma, pituitary and no tumour. VGG16 transfer learning reduced the trainable output to roughly 16,000 parameters.</p></div></article>
          <article><div><span>INPUT SHAPE</span><strong>224 × 224 × 3</strong><p>Every source image was resized and converted to a consistent three-channel tensor. This common input made heterogeneous resolutions compatible with the same feature-extraction pipeline.</p></div></article>
        </div>
      </section>

      <section class="case-section" aria-labelledby="data-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">DATA STRATEGY</p>
            <h2 id="data-title">More data helped. Better-controlled data mattered more.</h2>
          </div>
          <div><p>The final experiment used 5,320 MRI images with balanced class counts. A 70/20/10 strategy separated learning, model selection and final evaluation, while augmentation introduced controlled rotations, shifts, zoom and flips exclusively during training.</p></div>
        </div>
        <div class="dataset-bar" aria-label="Dataset distribution">
          <div class="train"><strong>3,720</strong><span>Training · 70%</span></div>
          <div class="validation"><strong>1,080</strong><span>Validation · 20%</span></div>
          <div class="test"><strong>520</strong><span>Test · 10%</span></div>
        </div>
        <figure class="research-figure">
          <button type="button" class="figure-zoom-trigger" data-lightbox-trigger aria-label="Enlarge MRI data augmentation examples">
            <img src="/images/projects/brain-tumour-mri/data-augmentation.png" alt="MRI image and augmented variants used during model training" />
            <span aria-hidden="true">Expand</span>
          </button>
          <figcaption>Controlled augmentation generated new training variants while preserving the original anatomy.</figcaption>
        </figure>
        <section class="dataset-sources" aria-labelledby="sources-title">
          <div>
            <p class="case-eyebrow">OPEN ACADEMIC DATA</p>
            <h3 id="sources-title">Where the MRI images came from.</h3>
            <p>The research combined publicly accessible MRI collections used for academic experimentation. Images from the different sources were reviewed, reorganised and filtered before entering the common training workflow.</p>
          </div>
          <div class="source-links">
            <a class="interactive-control neon-box-control premium-neon-control dataset-source-card" href="https://figshare.com/articles/dataset/brain_tumor_dataset/1512427" target="_blank" rel="noreferrer">
              <span>FIGSHARE</span><strong>Brain Tumor Dataset</strong><small>3,064 contrast-enhanced T1-weighted MRI images across three tumour classes. CC BY 4.0.</small>
              <span class="dataset-source-arrow neon-box-arrow" aria-hidden="true">↗</span>
            </a>
            <a class="interactive-control neon-box-control premium-neon-control dataset-source-card" href="https://www.kaggle.com/datasets/sartajbhuvaji/brain-tumor-classification-mri" target="_blank" rel="noreferrer">
              <span>KAGGLE</span><strong>Brain Tumor Classification (MRI)</strong><small>Public academic collection organised around glioma, meningioma, pituitary and no-tumour images.</small>
              <span class="dataset-source-arrow neon-box-arrow" aria-hidden="true">↗</span>
            </a>
            <a class="interactive-control neon-box-control premium-neon-control dataset-source-card" href="https://github.com/MohamedAliHabib/Brain-Tumor-Detection" target="_blank" rel="noreferrer">
              <span>GITHUB</span><strong>Brain Tumor Detection Dataset</strong><small>Open reference repository containing MRI samples and CNN experimentation material.</small>
              <span class="dataset-source-arrow neon-box-arrow" aria-hidden="true">↗</span>
            </a>
          </div>
        </section>
      </section>

      <section class="case-section" aria-labelledby="results-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">EXPERIMENTAL RESULTS</p>
          <h2 id="results-title">Strong class-level results, with visible room for improvement.</h2>
          <p>The four-class model reached 88.17% training accuracy. Class-level evaluation showed that glioma remained the most difficult category, reinforcing the effect of visual similarity and dataset quality.</p>
        </div>
        <figure class="benchmark-figure">
          <div class="benchmark-chart" role="group" aria-label="Comparison between target-class reference results and the predicted-label performance of the applied model">
            <section class="performance-card" aria-labelledby="comparison-chart-title">
              <div class="benchmark-panel-heading">
                <span>CLASS-LEVEL COMPARISON</span>
                <strong id="comparison-chart-title">Target benchmark vs predicted-label accuracy</strong>
                <div class="chart-legend" aria-label="Chart legend"><span><i class="target-key"></i>Target benchmark</span><span><i class="predicted-key"></i>Predicted label</span></div>
              </div>
              <div class="comparison-plot">
                {[
                  { label: "Meningioma", target: "74.0", predicted: "95.38" },
                  { label: "Glioma", target: "85.7", predicted: "81.54" },
                  { label: "Pituitary", target: "88.4", predicted: "87.66" },
                  { label: "No tumour", target: null, predicted: "93.08" },
                ].map(({ label, target, predicted }) => (
                  <div class="comparison-group">
                    <strong class="comparison-class">{label}</strong>
                    <div class="comparison-series">
                      <span class="series-name">Target</span>
                      <div class:list={["comparison-track", !target && "is-unavailable"]}>{target && <span class="target-bar" style={`--score:${target}%`}></span>}</div>
                      <strong class="series-value">{target ? `${target}%` : "N/A"}{!target && <small>Not evaluated</small>}</strong>
                    </div>
                    <div class="comparison-series">
                      <span class="series-name">Predicted</span>
                      <div class="comparison-track"><span class="predicted-bar" style={`--score:${predicted}%`}></span></div>
                      <strong class="series-value">{predicted}%</strong>
                    </div>
                  </div>
                ))}
                <div class="comparison-scale" aria-hidden="true"><div class="comparison-axis"><span>0</span><span>25</span><span>50</span><span>75</span><span>100</span></div><strong>Accuracy (%)</strong></div>
              </div>
            </section>
          </div>
          <figcaption>
            <strong>Impact of the applied techniques.</strong>
            <span>Data cleaning, class balancing and controlled augmentation produced a substantial improvement in prediction accuracy. Combined with transfer learning, the final model surpassed the initial benchmark in its strongest classes and added reliable detection of the no-tumour category.</span>
          </figcaption>
        </figure>
      </section>

      <section class="case-section application-section" aria-labelledby="application-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">FROM MODEL TO PRODUCT</p>
            <h2 id="application-title">A web interface made the model accessible beyond the notebook.</h2>
          </div>
          <div>
            <p>The trained Keras model was served through a Python and Flask backend. A browser client submitted an MRI image, received class probabilities and represented them numerically and through D3.js charts.</p>
            <p>This separated model inference from presentation and demonstrated how an experimental neural network could be integrated into a user-facing workflow.</p>
          </div>
        </div>
        <div class="app-flow" aria-label="Application architecture">
          <article><span aria-hidden="true"></span><strong>MRI upload</strong><small>Browser client</small></article>
          <article><span aria-hidden="true"></span><strong>Preprocess</strong><small>Python · Flask</small></article>
          <article><span aria-hidden="true"></span><strong>Inference</strong><small>VGG16 · Keras</small></article>
          <article><span aria-hidden="true"></span><strong>Probabilities</strong><small>JSON · D3.js</small></article>
        </div>
        <figure class="prediction-report">
          <button type="button" class="figure-zoom-trigger prediction-report-image" data-lightbox-trigger aria-label="Enlarge pituitary tumour MRI input">
            <img src="/images/projects/brain-tumour-mri/class-pituitary.png" alt="Pituitary tumour MRI selected as input for the four-class prediction" />
            <span aria-hidden="true">Expand</span>
          </button>
          <pre aria-label="Four-class prediction report">========================================
MRI CLASSIFICATION REPORT
========================================

INPUT DATA
FILE............. pituitary (654).png
INPUT TYPE....... MRI IMAGE
MODEL OUTPUT..... FOUR CLASSES

PREDICTION SCORES
MENINGIOMA....... 0.007176
NO TUMOUR........ 0.000283
GLIOMA........... 0.051178
PITUITARY........ 0.941363

PRIMARY CLASSIFICATION
LABEL............ PITUITARY
CONFIDENCE....... 94.1363%

REPORT STATUS.... COMPLETE
END OF REPORT
========================================</pre>
          <figcaption>Pituitary MRI input and the four probability values returned by the trained model.</figcaption>
        </figure>
      </section>

      <section class="case-section reflection-section" aria-labelledby="reflection-title">
        <div>
          <p class="case-eyebrow">ENGINEERING REFLECTION</p>
          <h2 id="reflection-title">The result was not just a classifier. It was a complete experimental system.</h2>
        </div>
        <div class="reflection-grid">
          <article><h3>What worked</h3><p>Dataset combination, augmentation and transfer learning produced reproducible multiclass predictions and enabled a functional application layer.</p></article>
          <article><h3>What remained difficult</h3><p>Image resolution, heterogeneous acquisition views and physical similarity between tumour classes constrained validation performance.</p></article>
          <article><h3>What comes next</h3><p>Higher-quality clinical datasets, specialist collaboration, stronger validation protocols and evaluation on additional medical image formats.</p></article>
        </div>
        <aside class="ethics-note"><strong>Responsible-use boundary</strong><p>This academic prototype was designed to explore decision support. Its predictions cannot replace multidisciplinary diagnosis by qualified radiology, oncology and neurology professionals.</p></aside>
      </section>

      <section class="case-section technology-section" aria-labelledby="technology-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">TECHNICAL FOUNDATION</p>
          <h2 id="technology-title">Tools behind the research.</h2>
          <p>The stack connected model development, accelerated experimentation, backend inference and browser-based visualisation in one end-to-end workflow.</p>
        </div>
        <div class="case-tags" aria-label="Core technologies">
          {project.technicalScope.map((item) => <span>{item}</span>)}
        </div>
      </section>

      <footer class="case-footer">
        <a href="/#projects" class="interactive-control neon-box-control project-return"><span class="neon-box-label">Back to projects</span><span class="neon-box-arrow" aria-hidden="true">↗</span></a>
      </footer>

      <dialog class="figure-lightbox" data-figure-lightbox aria-label="Expanded VGG16 architecture diagram">
        <div class="figure-lightbox-content">
          <button type="button" class="figure-lightbox-close" data-figure-close aria-label="Close enlarged image">×</button>
          <img data-lightbox-image src="/images/projects/brain-tumour-mri/vgg16-architecture.png" alt="Expanded research figure" />
        </div>
      </dialog>
    </main>
  ) : (
    <section class="relative isolate min-h-[calc(100svh-5rem)] overflow-hidden px-6 py-10 sm:px-10 lg:px-16">
      <nav aria-label="Project navigation" class="project-navigation flex flex-wrap gap-3">
        <a href="/#projects" class="interactive-control project-icon-control" aria-label="Back to projects" title="Back to projects">
          <svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20 12H4" />
            <path d="m10 6-6 6 6 6" />
          </svg>
        </a>
      </nav>

      <article class="mx-auto mt-24 max-w-4xl">
        <p class="text-sm font-medium tracking-[0.22em] text-violet-300">PROJECT CASE STUDY</p>
        <p class="mt-5 text-sm text-slate-400">{project.location}</p>
        <h1 class="interactive-heading mt-4 text-4xl font-semibold tracking-tight text-slate-400 sm:text-6xl">{project.title}</h1>
        <p class="mt-8 max-w-3xl text-xl leading-9 text-slate-300">{project.summary}</p>

        <div class="mt-16 grid gap-12 border-t border-violet-300/15 pt-10 sm:grid-cols-2">
          <section>
            <p class="text-sm font-medium tracking-[0.18em] text-violet-300">CONTRIBUTION</p>
            <p class="mt-4 leading-8 text-slate-400">{project.contribution}</p>
          </section>
          <section>
            <p class="text-sm font-medium tracking-[0.18em] text-violet-300">OUTCOME</p>
            <p class="mt-4 leading-8 text-slate-400">{project.outcome}</p>
          </section>
        </div>

        <section class="mt-14 border-t border-violet-300/15 pt-10">
          <p class="text-sm font-medium tracking-[0.18em] text-violet-300">TECHNICAL SCOPE</p>
          <div class="mt-5 flex flex-wrap gap-3">
            {project.technicalScope.map((item) => <span class="tech-scope-item">{item}</span>)}
          </div>
        </section>
      </article>
    </section>
  )}
</BaseLayout>

<script>
  const zoomTriggers = document.querySelectorAll<HTMLButtonElement>("[data-lightbox-trigger]");
  const lightbox = document.querySelector<HTMLDialogElement>("[data-figure-lightbox]");
  const closeButton = document.querySelector<HTMLButtonElement>("[data-figure-close]");
  const lightboxImage = document.querySelector<HTMLImageElement>("[data-lightbox-image]");
  let activeTrigger: HTMLButtonElement | null = null;

  const closeLightbox = () => {
    if (!lightbox?.open) return;
    lightbox.close();
  };

  zoomTriggers.forEach((trigger) => {
    trigger.addEventListener("click", () => {
      const sourceImage = trigger.querySelector<HTMLImageElement>("img");
      if (!lightbox || !lightboxImage || !sourceImage) return;
      activeTrigger = trigger;
      lightboxImage.src = sourceImage.currentSrc || sourceImage.src;
      lightboxImage.alt = `Expanded ${sourceImage.alt}`;
      lightbox.setAttribute("aria-label", lightboxImage.alt);
      lightbox.showModal();
    });
  });
  closeButton?.addEventListener("click", closeLightbox);
  lightbox?.addEventListener("close", () => activeTrigger?.focus());
  lightbox?.addEventListener("click", (event) => {
    if (event.target === lightbox) closeLightbox();
  });

  if (document.querySelector(".case-study")) {
    let scrollEffectTimer: number | undefined;
    const updateScrollEffect = () => {
      document.documentElement.classList.add("case-is-scrolling");
      window.clearTimeout(scrollEffectTimer);
      scrollEffectTimer = window.setTimeout(() => document.documentElement.classList.remove("case-is-scrolling"), 150);
    };

    window.addEventListener("scroll", updateScrollEffect, { passive: true });
  }
</script>

<style>
  .case-study {
    --case-border: rgba(196, 181, 253, 0.22);
    --case-panel: rgba(13, 9, 22, 0.72);
    background:
      radial-gradient(circle at 82% 7%, rgba(124, 58, 237, 0.14), transparent 24rem),
      radial-gradient(circle at 12% 33%, rgba(34, 211, 238, 0.055), transparent 25rem),
      #000;
    color: rgb(203 213 225);
    overflow: hidden;
  }

  .case-hero,
  .case-section,
  .case-footer {
    margin-inline: auto;
    max-width: 86rem;
    padding-inline: clamp(1.35rem, 5vw, 5rem);
  }

  .case-hero {
    min-height: calc(100svh - 5rem);
    padding-bottom: clamp(5rem, 10vw, 9rem);
    padding-top: 2.5rem;
  }

  .case-hero-grid {
    align-items: center;
    display: grid;
    gap: clamp(3rem, 7vw, 8rem);
    grid-template-columns: minmax(0, 1.03fr) minmax(22rem, 0.97fr);
    min-height: calc(100svh - 12rem);
    padding-top: 2rem;
  }

  .case-eyebrow {
    color: rgb(196 181 253);
    font-size: 0.76rem;
    font-weight: 700;
    letter-spacing: 0.2em;
  }

  .case-hero h1 {
    color: rgb(226 232 240);
    font-size: clamp(2.1rem, 3.7vw, 3.6rem);
    font-weight: 620;
    letter-spacing: -0.055em;
    line-height: 0.94;
    margin-top: 1.4rem;
    max-width: 12ch;
    text-wrap: balance;
  }

  .case-lead {
    color: rgb(148 163 184);
    font-size: clamp(1.12rem, 1.65vw, 1.4rem);
    line-height: 1.75;
    margin-top: 2rem;
    max-width: 40rem;
  }

  .case-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
    margin-top: 2rem;
  }

  .case-tags span {
    border: 1px solid rgba(196, 181, 253, 0.23);
    border-radius: 999px;
    color: rgb(196 181 253);
    font-size: 0.72rem;
    font-weight: 650;
    letter-spacing: 0.08em;
    padding: 0.52rem 0.72rem;
    text-transform: uppercase;
  }

  .hero-visual {
    margin: 0;
    position: relative;
  }

  .scan-frame {
    background: #030305;
    border: 1px solid rgba(196, 181, 253, 0.32);
    border-radius: 1.6rem;
    box-shadow: 0 0 4rem rgba(124, 58, 237, 0.12);
    overflow: hidden;
    padding: clamp(0.8rem, 2vw, 1.5rem);
    position: relative;
  }

  .scan-frame::after {
    background: linear-gradient(135deg, transparent 42%, rgba(167, 139, 250, 0.11));
    content: "";
    inset: 0;
    pointer-events: none;
    position: absolute;
  }

  .scan-frame img {
    aspect-ratio: 1.12;
    filter: grayscale(1) contrast(1.18) hue-rotate(225deg);
    height: 100%;
    object-fit: cover;
    width: 100%;
  }

  .scan-line {
    animation: scan 5s ease-in-out infinite;
    background: linear-gradient(90deg, transparent, rgba(196, 181, 253, 0.8), transparent);
    box-shadow: 0 0 1rem rgba(167, 139, 250, 0.5);
    height: 1px;
    left: 1rem;
    position: absolute;
    pointer-events: none;
    right: 1rem;
    top: 18%;
    z-index: 2;
  }

  @keyframes scan { 0%, 100% { transform: translateY(0); opacity: 0.35; } 50% { transform: translateY(20rem); opacity: 0.9; } }

  .hero-visual figcaption,
  .research-figure figcaption {
    color: rgb(100 116 139);
    font-size: 0.75rem;
    letter-spacing: 0.04em;
    margin-top: 0.85rem;
  }

  .case-section {
    border-top: 1px solid var(--case-border);
    padding-bottom: clamp(5rem, 9vw, 9rem);
    padding-top: clamp(5rem, 9vw, 9rem);
  }

  .section-heading {
    max-width: 55rem;
  }

  .section-heading h2,
  .split-copy h2,
  .reflection-section h2 {
    color: rgb(226 232 240);
    font-size: clamp(2.1rem, 3.7vw, 3.6rem);
    font-weight: 600;
    letter-spacing: -0.045em;
    line-height: 1.05;
    margin-top: 1.25rem;
    text-wrap: balance;
  }

  .section-heading > p:last-child,
  .split-copy > div:last-child p,
  .reflection-grid p {
    color: rgb(148 163 184);
    font-size: 1.02rem;
    line-height: 1.85;
  }

  .section-heading > p:last-child { margin-top: 1.6rem; }
  .compact-heading { max-width: 64rem; }

  .metric-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin-top: 3.2rem;
  }

  .metric-grid article,
  .class-grid article {
    background: linear-gradient(145deg, rgba(17, 12, 28, 0.8), rgba(3, 3, 5, 0.65));
    border: 1px solid var(--case-border);
    border-radius: 1.1rem;
    padding: clamp(1.35rem, 3vw, 2.1rem);
  }

  .metric-grid article {
    align-content: center;
    min-height: 9.5rem;
    padding: clamp(1.1rem, 2vw, 1.45rem);
  }

  .metric-grid strong {
    color: rgb(196 181 253);
    display: block;
    font-size: clamp(2rem, 3.25vw, 3rem);
    letter-spacing: -0.05em;
  }

  .metric-grid span {
    color: rgb(148 163 184);
    display: block;
    line-height: 1.55;
    margin-top: 0.65rem;
  }

  .source-note {
    color: rgb(100 116 139);
    font-size: 0.78rem;
    line-height: 1.6;
    margin-top: 1.25rem;
  }

  .source-note a {
    color: rgb(196 181 253);
    text-decoration: underline;
    text-decoration-color: rgba(196, 181, 253, 0.38);
    text-underline-offset: 0.22em;
  }

  .split-copy {
    display: grid;
    gap: clamp(2rem, 7vw, 8rem);
    grid-template-columns: minmax(0, 0.95fr) minmax(0, 1.05fr);
  }

  .split-copy > div:last-child p + p { margin-top: 1.3rem; }

  .class-grid {
    display: grid;
    gap: 1rem;
    margin-top: 3.5rem;
  }

  .class-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); }

  .class-grid article {
    overflow: hidden;
    padding: 0;
  }

  .class-grid .class-image-trigger > img {
    aspect-ratio: 1;
    display: block;
    filter: grayscale(1) contrast(1.08);
    object-fit: cover;
    transition: filter 240ms ease, transform 350ms cubic-bezier(0.22, 1, 0.36, 1);
    width: 100%;
  }

  .class-grid article:hover .class-image-trigger > img,
  .class-grid .class-image-trigger:focus-visible > img {
    filter: grayscale(0.78) contrast(1.12) sepia(0.08) hue-rotate(220deg);
    transform: scale(1.025);
  }

  .class-grid article > div {
    border-top: 1px solid var(--case-border);
    min-height: 11rem;
    padding: 1.4rem;
    position: relative;
  }

  .class-grid h3 {
    color: rgb(196 181 253);
    font-size: 0.8rem;
    letter-spacing: 0.13em;
  }

  .class-grid p {
    color: rgb(148 163 184);
    font-size: 0.9rem;
    line-height: 1.65;
    margin-top: 0.75rem;
  }

  .pipeline,
  .experiment-grid,
  .reflection-grid {
    --timeline-gutter: clamp(2.2rem, 4vw, 3rem);
    display: grid;
    gap: 0;
    grid-template-columns: 1fr;
    list-style: none;
    margin: 3.5rem 0 0;
    max-width: 68rem;
    padding: 0 0 0 var(--timeline-gutter);
    position: relative;
  }

  .pipeline::before,
  .experiment-grid::before,
  .reflection-grid::before {
    background: linear-gradient(180deg, transparent, rgb(139 92 246) 8%, rgb(196 181 253) 50%, rgb(139 92 246) 92%, transparent);
    box-shadow: 0 0 0.75rem rgba(139, 92, 246, 0.75), 0 0 1.25rem rgba(167, 139, 250, 0.24);
    bottom: 0.35rem;
    content: "";
    left: 0.38rem;
    position: absolute;
    top: 0.35rem;
    width: 1px;
  }

  :global(html.case-is-scrolling) .pipeline::before,
  :global(html.case-is-scrolling) .experiment-grid::before,
  :global(html.case-is-scrolling) .reflection-grid::before {
    animation: vertical-neon-scroll 5.5s linear infinite;
    background: linear-gradient(180deg, transparent 0%, rgb(139 92 246) 8%, rgb(139 92 246) 38%, rgba(255, 255, 255, 0.95) 49%, rgb(196 181 253) 54%, rgb(139 92 246) 92%, transparent 100%);
    background-size: 100% 220%;
  }

  @keyframes vertical-neon-scroll {
    from { background-position: 0 100%; }
    to { background-position: 0 -120%; }
  }

  .pipeline li,
  .experiment-grid article,
  .reflection-grid article {
    background: transparent;
    border: 0;
    border-radius: 0;
    display: grid;
    gap: clamp(0.75rem, 3vw, 2rem);
    grid-template-columns: minmax(6rem, 0.24fr) minmax(0, 1fr);
    min-height: auto;
    padding: 0 0 clamp(2.1rem, 4vw, 3.2rem);
    position: relative;
  }

  .pipeline li::before,
  .experiment-grid article::before,
  .reflection-grid article::before {
    background: #030305;
    border: 1px solid rgb(196 181 253);
    border-radius: 999px;
    box-shadow: 0 0 0.55rem rgba(196, 181, 253, 0.9), 0 0 1.1rem rgba(139, 92, 246, 0.38);
    content: "";
    height: 0.72rem;
    left: calc(0.02rem - var(--timeline-gutter));
    position: absolute;
    top: 0.22rem;
    width: 0.72rem;
  }

  .pipeline li:last-child,
  .experiment-grid article:last-child,
  .reflection-grid article:last-child { padding-bottom: 0; }

  .experiment-grid article,
  .reflection-grid article { grid-template-columns: 1fr; }

  .pipeline span,
  .experiment-grid span,
  .reflection-grid h3 {
    color: rgb(139 92 246);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.16em;
  }

  .pipeline strong,
  .experiment-grid strong {
    color: rgb(226 232 240);
    display: block;
    font-size: 1.05rem;
  }

  .experiment-grid strong { margin-top: 0.55rem; }

  .pipeline p,
  .experiment-grid p,
  .reflection-grid p {
    color: rgb(148 163 184);
    font-size: 0.92rem;
    line-height: 1.7;
    margin-top: 0.65rem;
    max-width: 46rem;
  }

  .research-figure {
    background: #050507;
    border: 1px solid var(--case-border);
    border-radius: 1.2rem;
    margin: 3rem 0 0;
    overflow: hidden;
    padding: clamp(0.75rem, 2vw, 1.4rem);
  }

  .research-figure img {
    background: #fff;
    border-radius: 0.7rem;
    display: block;
    height: auto;
    width: 100%;
  }

  .wide-figure img { filter: grayscale(0.25) saturate(0.75) hue-rotate(205deg); }

  .figure-zoom-trigger {
    background: transparent;
    border: 0;
    cursor: zoom-in;
    display: block;
    padding: 0;
    position: relative;
    width: 100%;
  }

  .figure-zoom-trigger::after {
    background: linear-gradient(135deg, transparent 45%, rgba(5, 5, 8, 0.86));
    border-radius: 0.7rem;
    content: "";
    inset: 0;
    opacity: 0;
    pointer-events: none;
    position: absolute;
    transition: opacity 180ms ease;
  }

  .figure-zoom-trigger > span {
    bottom: 1rem;
    color: rgb(221 214 254);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.13em;
    opacity: 0;
    position: absolute;
    right: 1rem;
    text-transform: uppercase;
    transition: opacity 180ms ease;
    z-index: 1;
  }

  .figure-zoom-trigger:hover::after,
  .figure-zoom-trigger:focus-visible::after,
  .figure-zoom-trigger:hover > span,
  .figure-zoom-trigger:focus-visible > span { opacity: 1; }

  .figure-zoom-trigger:focus-visible {
    border-radius: 0.7rem;
    outline: 2px solid rgb(196 181 253);
    outline-offset: 0.25rem;
  }

  .figure-lightbox {
    background: transparent;
    border: 0;
    height: 100dvh;
    margin: 0;
    max-height: none;
    max-width: none;
    padding: clamp(1rem, 4vw, 3rem);
    width: 100vw;
  }

  .figure-lightbox::backdrop {
    backdrop-filter: blur(12px);
    background: rgba(0, 0, 0, 0.84);
  }

  .figure-lightbox-content {
    align-items: center;
    display: flex;
    height: 100%;
    justify-content: center;
    margin: auto;
    max-width: 92rem;
    position: relative;
  }

  .figure-lightbox-content > img {
    background: #fff;
    border: 1px solid rgba(196, 181, 253, 0.42);
    border-radius: 0.9rem;
    box-shadow: 0 0 4rem rgba(124, 58, 237, 0.18);
    display: block;
    max-height: calc(100dvh - 5rem);
    max-width: 100%;
    object-fit: contain;
  }

  .figure-lightbox-close {
    align-items: center;
    background: rgba(8, 5, 14, 0.92);
    border: 1px solid rgba(196, 181, 253, 0.48);
    border-radius: 999px;
    color: rgb(237 233 254);
    cursor: pointer;
    display: flex;
    font-size: 1.5rem;
    height: 2.6rem;
    justify-content: center;
    line-height: 1;
    position: absolute;
    right: -0.8rem;
    top: 0;
    width: 2.6rem;
    z-index: 2;
  }

  .figure-lightbox-close:hover,
  .figure-lightbox-close:focus-visible {
    border-color: rgb(196 181 253);
    box-shadow: 0 0 1.2rem rgba(167, 139, 250, 0.3);
    outline: none;
  }

  .dataset-sources {
    align-items: start;
    display: grid;
    gap: clamp(2rem, 7vw, 7rem);
    grid-template-columns: minmax(0, 0.75fr) minmax(0, 1.25fr);
    margin-top: clamp(4rem, 8vw, 7rem);
  }

  .dataset-sources h3 {
    color: rgb(226 232 240);
    font-size: clamp(1.7rem, 3vw, 2.6rem);
    font-weight: 600;
    letter-spacing: -0.035em;
    line-height: 1.1;
    margin-top: 1rem;
  }

  .dataset-sources > div:first-child > p:last-child {
    color: rgb(148 163 184);
    line-height: 1.8;
    margin-top: 1.2rem;
  }

  .source-links {
    display: grid;
    gap: 0.85rem;
  }

  .source-links a.dataset-source-card {
    align-content: center;
    display: grid;
    gap: 0.45rem;
    min-height: 8.5rem;
    padding: 1.35rem 3rem 1.35rem 1.4rem;
    position: relative;
  }

  .source-links .dataset-source-arrow {
    color: rgb(196 181 253);
    letter-spacing: 0;
    position: absolute;
    right: 1.25rem;
    top: 50%;
    transform: translateY(-50%);
    transition: color 180ms ease, transform 180ms ease;
  }

  .source-links a.dataset-source-card:hover .dataset-source-arrow,
  .source-links a.dataset-source-card:focus-visible .dataset-source-arrow {
    transform: translate(0.12rem, calc(-50% - 0.12rem));
  }

  .source-links a > span:first-child { color: rgb(139 92 246); font-size: 0.67rem; font-weight: 700; letter-spacing: 0.16em; }
  .source-links strong { color: rgb(226 232 240); font-size: 1rem; }
  .source-links small { color: rgb(100 116 139); line-height: 1.6; max-width: 44rem; }

  .dataset-bar {
    display: grid;
    gap: 0.25rem;
    grid-template-columns: 7fr 2fr 1fr;
    margin-top: 2.5rem;
    max-width: 68rem;
  }

  .dataset-bar > div {
    background: rgba(124, 58, 237, 0.32);
    border: 1px solid rgba(196, 181, 253, 0.28);
    min-height: 4.5rem;
    padding: 0.65rem 0.9rem;
  }

  .dataset-bar > div:first-child { border-radius: 1rem 0 0 1rem; }
  .dataset-bar > div:last-child { border-radius: 0 1rem 1rem 0; }
  .dataset-bar .validation { background: rgba(139, 92, 246, 0.21); }
  .dataset-bar .test { background: rgba(34, 211, 238, 0.11); }
  .dataset-bar strong { color: rgb(226 232 240); display: block; font-size: clamp(1.25rem, 2vw, 1.8rem); }
  .dataset-bar span { color: rgb(196 181 253); display: block; font-size: 0.68rem; margin-top: 0.35rem; }

  .benchmark-figure { margin: 4rem 0 0; max-width: 72rem; }

  .benchmark-chart {
    display: block;
  }

  .performance-card {
    background: linear-gradient(145deg, rgba(16, 11, 27, 0.66), rgba(3, 3, 5, 0.82));
    border: 1px solid var(--case-border);
    border-radius: 1.15rem;
    min-width: 0;
    padding: clamp(1.4rem, 3vw, 2.2rem);
  }

  .benchmark-panel-heading { border-bottom: 1px solid rgba(196, 181, 253, 0.15); margin-bottom: 1.8rem; padding-bottom: 1.2rem; }
  .benchmark-panel-heading > span { color: rgb(139 92 246); display: block; font-size: 0.67rem; font-weight: 700; letter-spacing: 0.16em; }
  .benchmark-panel-heading > strong { color: rgb(226 232 240); display: block; font-size: 1.1rem; margin-top: 0.45rem; }
  .chart-legend { display: flex; flex-wrap: wrap; gap: 1.2rem; margin-top: 1rem; }
  .chart-legend span { align-items: center; color: rgb(148 163 184); display: inline-flex; font-size: 0.72rem; gap: 0.45rem; letter-spacing: 0; }
  .chart-legend i { display: inline-block; height: 0.45rem; width: 1.5rem; }
  .target-key,
  .target-bar { background: rgb(226 232 240); }
  .predicted-key,
  .predicted-bar { background: rgb(139 92 246); }
  .comparison-plot { --label-column: 7rem; --value-column: 5.5rem; }
  .comparison-scale { margin: 1rem var(--value-column) 0 calc(var(--label-column) + 1rem); }
  .comparison-scale > strong { color: rgb(196 181 253); display: block; font-size: 0.62rem; font-weight: 600; letter-spacing: 0.08em; margin-top: 0.4rem; text-align: center; text-transform: uppercase; }
  .comparison-axis { color: rgb(100 116 139); display: flex; font-size: 0.62rem; justify-content: space-between; }
  .comparison-group { border-top: 1px solid rgba(196, 181, 253, 0.09); margin-top: 1.1rem; padding-top: 1.1rem; }
  .comparison-class { color: rgb(226 232 240); display: block; font-size: 0.82rem; margin-bottom: 0.65rem; }
  .comparison-series { align-items: center; display: grid; gap: 1rem; grid-template-columns: var(--label-column) minmax(0, 1fr) var(--value-column); }
  .comparison-series + .comparison-series { margin-top: 0.55rem; }
  .series-name { color: rgb(148 163 184); font-size: 0.7rem; }
  .comparison-track { background: repeating-linear-gradient(90deg, rgba(196, 181, 253, 0.08) 0 1px, transparent 1px 25%); border-bottom: 1px solid rgba(196, 181, 253, 0.18); height: 0.72rem; overflow: hidden; position: relative; }
  .comparison-track > span { box-shadow: 0 0 0.65rem rgba(167, 139, 250, 0.35); display: block; height: 100%; width: var(--score); }
  .comparison-track.is-unavailable { background: repeating-linear-gradient(135deg, transparent 0 5px, rgba(148, 163, 184, 0.08) 5px 6px); }
  .series-value { color: rgb(226 232 240); font-size: 0.78rem; text-align: right; }
  .series-value small { color: rgb(139 92 246); display: block; font-size: 0.48rem; font-weight: 600; letter-spacing: 0.08em; margin-top: 0.12rem; text-transform: uppercase; }

  .benchmark-figure figcaption { color: rgb(148 163 184); line-height: 1.75; margin-top: 1.35rem; max-width: 66rem; }
  .benchmark-figure figcaption strong { color: rgb(196 181 253); display: block; font-size: 0.78rem; letter-spacing: 0.08em; text-transform: uppercase; }
  .benchmark-figure figcaption span { display: block; margin-top: 0.55rem; }

  .app-flow {
    --flow-line: rgba(167, 139, 250, 0.38);
    display: grid;
    gap: clamp(1.5rem, 4vw, 4rem);
    grid-template-columns: repeat(4, minmax(0, 1fr));
    margin-top: 3.5rem;
    position: relative;
  }

  .app-flow::before {
    background: linear-gradient(90deg, transparent, var(--flow-line) 7%, var(--flow-line) 93%, transparent);
    content: "";
    height: 1px;
    left: 0;
    position: absolute;
    right: 0;
    top: 0.42rem;
  }

  .app-flow article {
    min-height: 6.5rem;
    padding-top: 1.9rem;
    position: relative;
  }

  .app-flow span {
    background: rgb(196 181 253);
    border: 3px solid #000;
    border-radius: 999px;
    box-shadow: 0 0 0 1px rgba(196, 181, 253, 0.55), 0 0 1rem rgba(167, 139, 250, 0.75);
    display: block;
    height: 0.8rem;
    left: 0;
    position: absolute;
    top: 0;
    width: 0.8rem;
  }

  .app-flow strong { color: rgb(226 232 240); display: block; }
  .app-flow small { color: rgb(100 116 139); display: block; margin-top: 0.35rem; }
  .prediction-report {
    display: grid;
    gap: 1.25rem;
    margin: 3rem auto 0;
    max-width: 46rem;
  }

  .prediction-report-image { background: #020203; border: 1px solid var(--case-border); border-radius: 1.1rem; overflow: hidden; padding: clamp(1rem, 3vw, 2rem); }
  .prediction-report-image img { display: block; height: auto; margin-inline: auto; max-height: 24rem; max-width: 100%; object-fit: contain; width: auto; }
  .prediction-report pre {
    background: #000;
    border: 1px solid var(--case-border);
    border-radius: 1.1rem;
    color: rgb(226 232 240);
    font-family: Consolas, "Courier New", monospace;
    font-size: clamp(0.7rem, 1.5vw, 0.88rem);
    line-height: 1.7;
    margin: 0;
    overflow-x: auto;
    padding: clamp(1.1rem, 3vw, 1.7rem);
    white-space: pre;
  }

  .prediction-report figcaption { color: rgb(100 116 139); font-size: 0.72rem; line-height: 1.55; padding: 0 0.2rem; }

  .ethics-note {
    border-left: 0;
    margin-top: 3rem;
    max-width: 64rem;
    padding: 0.2rem 0;
  }

  .ethics-note strong { color: rgb(196 181 253); font-size: 0.78rem; letter-spacing: 0.12em; text-transform: uppercase; }
  .ethics-note p { color: rgb(148 163 184); line-height: 1.75; margin-top: 0.65rem; }

  .case-footer { border-top: 1px solid var(--case-border); padding-bottom: 6rem; padding-top: 4rem; }
  .project-return { max-width: 24rem; }

  .project-navigation {
    width: fit-content;
  }

  .project-icon-control {
    align-items: center;
    border: 1px solid rgba(196, 181, 253, 0.3);
    border-radius: 999px;
    color: rgb(148 163 184);
    display: inline-flex;
    height: 2.65rem;
    justify-content: center;
    padding: 0.35rem;
    width: 2.65rem;
  }

  .project-icon-control :global(svg) {
    height: 1.7rem;
    min-height: 1.7rem;
    min-width: 1.7rem;
    transition: filter 180ms ease, transform 180ms ease;
    width: 1.7rem;
  }

  .interactive-control.project-icon-control:hover {
    animation: nav-link-presence 520ms cubic-bezier(0.22, 1, 0.36, 1);
    background: transparent;
    border-color: rgb(196 181 253);
    box-shadow: none;
    color: rgb(196 181 253);
    transform: none;
  }

  .interactive-control.project-icon-control:hover :global(svg) {
    filter: drop-shadow(0 0 5px rgba(167, 139, 250, 0.5)) drop-shadow(0 0 10px rgba(103, 232, 249, 0.12));
    transform: translateY(-1px);
  }

  .interactive-control.project-icon-control:active {
    border-color: rgb(196 181 253);
    box-shadow: none;
    color: rgb(196 181 253);
  }

  .tech-scope-item {
    border: 1px solid rgba(221, 214, 254, 0.3);
    border-radius: 0.7rem;
    background: rgba(167, 139, 250, 0.1);
    color: rgb(221 214 254);
    cursor: default;
    font-size: 0.875rem;
    font-weight: 600;
    padding: 0.55rem 0.8rem;
  }

  @media (max-width: 900px) {
    .case-hero-grid,
    .split-copy { grid-template-columns: 1fr; }
    .case-hero-grid { min-height: auto; padding-top: 5rem; }
    .hero-visual { max-width: 38rem; }
    .dataset-sources { grid-template-columns: 1fr; }
    .class-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .pipeline,
    .experiment-grid,
    .reflection-grid { max-width: 42rem; }
    .app-flow { gap: 0; grid-template-columns: 1fr; padding-left: 1.6rem; }
    .app-flow::before { background: linear-gradient(180deg, transparent, var(--flow-line) 7%, var(--flow-line) 93%, transparent); bottom: 0; height: auto; left: 0.38rem; right: auto; top: 0; width: 1px; }
    :global(html.case-is-scrolling) .app-flow::before {
      animation: vertical-neon-scroll 5.5s linear infinite;
      background: linear-gradient(180deg, transparent 0%, rgb(139 92 246) 8%, rgb(139 92 246) 38%, rgba(255, 255, 255, 0.95) 49%, rgb(196 181 253) 54%, rgb(139 92 246) 92%, transparent 100%);
      background-size: 100% 220%;
    }
    .app-flow article { min-height: 5.6rem; padding-left: 0.9rem; padding-top: 0; }
    .app-flow span { left: -1.6rem; top: 0.15rem; }
  }

  @media (max-width: 48rem) {
    .source-links a.dataset-source-card:hover .dataset-source-arrow,
    .source-links a.dataset-source-card:focus-visible .dataset-source-arrow,
    .source-links a.dataset-source-card:active .dataset-source-arrow {
      transform: translateY(-50%) scaleX(1.08);
      transform-origin: right center;
    }
  }

  @media (max-width: 640px) {
    .case-hero { min-height: auto; padding-top: 1.5rem; }
    .case-hero-grid { padding-top: 4rem; }
    .case-hero h1,
    .section-heading h2,
    .split-copy h2,
    .reflection-section h2 { font-size: clamp(2rem, 9vw, 2.75rem); }
    .metric-grid,
    .class-grid { grid-template-columns: 1fr; }
    .metric-grid article { min-height: 7.5rem; }
    .pipeline li,
    .experiment-grid article,
    .reflection-grid article { gap: 0.45rem; grid-template-columns: 1fr; }
    .dataset-bar { gap: 1px; grid-template-columns: 7fr 2fr 1fr; }
    .dataset-bar > div { min-height: 3.85rem; min-width: 0; overflow: hidden; padding: 0.55rem 0.28rem; }
    .dataset-bar > div:first-child { border-radius: 1rem 0 0 1rem; }
    .dataset-bar > div:last-child { border-radius: 0 1rem 1rem 0; }
    .dataset-bar strong { font-size: clamp(0.72rem, 4.6vw, 1.3rem); letter-spacing: -0.035em; }
    .dataset-bar span { font-size: 0; margin-top: 0.45rem; }
    .dataset-bar .train span::after,
    .dataset-bar .validation span::after,
    .dataset-bar .test span::after { font-size: 0.58rem; letter-spacing: 0; }
    .dataset-bar .train span::after { content: "70%"; }
    .dataset-bar .validation span::after { content: "20%"; }
    .dataset-bar .test span::after { content: "10%"; }
    .performance-card { padding: 1.2rem; }
    .comparison-plot { --label-column: 4rem; --value-column: 4.2rem; }
    .comparison-scale { margin-left: calc(var(--label-column) + 0.55rem); }
    .comparison-series { gap: 0.55rem; }
    .series-name { font-size: 0.62rem; }
    .series-value { font-size: 0.68rem; }
    .research-figure { border-radius: 0.9rem; margin-top: 2rem; }
    .figure-zoom-trigger > span { opacity: 1; }
    .figure-lightbox { padding: 0.8rem; }
    .figure-lightbox-content > img { max-height: calc(100dvh - 2rem); }
    .figure-lightbox-close { right: 0.35rem; top: 0.35rem; }
    .scan-line { display: none; }
  }

  @media (prefers-reduced-motion: reduce) {
    .scan-line,
    :global(html.case-is-scrolling) .pipeline::before,
    :global(html.case-is-scrolling) .experiment-grid::before,
    :global(html.case-is-scrolling) .reflection-grid::before,
    :global(html.case-is-scrolling) .app-flow::before { animation: none; }
  }
</style>
