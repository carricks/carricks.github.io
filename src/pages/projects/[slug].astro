---
import { projects } from "../../data/projects";
import BaseLayout from "../../layouts/BaseLayout.astro";
import FloatingBackControl from "../../components/common/FloatingBackControl.astro";

export function getStaticPaths() {
  return projects.map((project) => ({
    params: { slug: project.slug },
    props: { project },
  }));
}

const { project } = Astro.props;
const isResearchCaseStudy = project.slug === "brain-tumour-mri-classification";
const isGridStabilityCaseStudy = project.slug === "grid-contingency-analysis";
const isRenewableControlCaseStudy = project.slug === "renewable-plant-control";
---

<BaseLayout title={`${project.title} | Diego Carrillo`} description={project.summary}>
  {isResearchCaseStudy ? (
    <main class="case-study">
      <section class="case-hero">
        <FloatingBackControl href="/#projects" label="Back to projects" />

        <div class="case-hero-grid">
          <div class="case-hero-copy">
            <p class="case-eyebrow">MASTER'S RESEARCH · COMPUTER VISION</p>
            <h1>Classifying brain tumours from MRI.</h1>
            <p class="case-lead">A complete research prototype that moved from heterogeneous medical images to a convolutional model covering four classes and a usable web interface.</p>
          </div>

          <figure class="hero-visual">
            <div class="scan-frame">
              <button type="button" class="figure-zoom-trigger" data-lightbox-trigger aria-label="Enlarge preprocessed MRI samples">
                <img src="/images/projects/brain-tumour-mri/mri-preprocessing.png" alt="Preprocessed MRI samples showing brain images with and without tumours" />
                <span aria-hidden="true">Expand</span>
              </button>
              <div class="scan-line" aria-hidden="true"></div>
            </div>
            <figcaption>Preprocessed MRI samples from the experimental workflow.</figcaption>
          </figure>
        </div>
      </section>

      <section class="case-section impact-section" aria-labelledby="impact-title">
        <div class="section-heading">
          <p class="case-eyebrow">WHY IT MATTERS</p>
          <h2 id="impact-title">A difficult visual problem with real clinical consequences.</h2>
          <p>Brain and central nervous system tumours are uncommon compared with many cancers, yet their location and heterogeneity make assessment especially demanding. The project explored how computer vision could support image review without replacing specialist judgement.</p>
        </div>
        <div class="metric-grid">
          <article><strong>321,731</strong><span>New brain and CNS cancer cases worldwide in 2022</span></article>
          <article><strong>248,500</strong><span>Estimated deaths worldwide in 2022</span></article>
          <article><strong>4 Classes</strong><span>Meningioma, glioma, pituitary and no tumour</span></article>
        </div>
        <p class="source-note">Global context: <a href="https://gco.iarc.who.int/media/globocan/factsheets/cancers/31-brain-central-nervous-system-fact-sheet.pdf" target="_blank" rel="noreferrer">IARC Global Cancer Observatory, GLOBOCAN 2022</a>. The prototype is research work and is not a validated medical diagnostic device.</p>
      </section>

      <section class="case-section" aria-labelledby="challenge-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">THE CHALLENGE</p>
            <h2 id="challenge-title">Teaching a model to distinguish visually similar patterns.</h2>
          </div>
          <div>
            <p>MRI datasets combine different resolutions, anatomical views and acquisition conditions. Tumours may also share location, shape and texture, making the boundary between classes difficult to learn.</p>
            <p>The work therefore focused as much on data quality and experimental discipline as on model architecture: combining public datasets, removing samples with insufficient quality, balancing classes and keeping test images outside augmentation.</p>
          </div>
        </div>
        <div class="class-grid" aria-label="Classification targets">
          {[
            { title: "MENINGIOMA", copy: "Tumour arising from the meninges surrounding the brain.", image: "class-meningioma.png" },
            { title: "GLIOMA", copy: "Tumour originating in glial cells, with highly variable appearance.", image: "class-glioma.png" },
            { title: "PITUITARY", copy: "Tumour located around the pituitary gland at the base of the brain.", image: "class-pituitary.png" },
            { title: "NO TUMOUR", copy: "MRI samples without evidence of the three target tumour classes.", image: "class-no-tumour.png" },
          ].map(({ title, copy, image }) => (
            <article>
              <button type="button" class="figure-zoom-trigger class-image-trigger" data-lightbox-trigger aria-label={`Enlarge MRI example classified as ${title.toLowerCase()}`}>
                <img src={`/images/projects/brain-tumour-mri/${image}`} alt={`MRI example classified as ${title.toLowerCase()}`} />
                <span aria-hidden="true">Expand</span>
              </button>
              <div><h3>{title}</h3><p>{copy}</p></div>
            </article>
          ))}
        </div>
      </section>

      <section class="case-section" aria-labelledby="pipeline-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">RESEARCH PIPELINE</p>
          <h2 id="pipeline-title">From raw images to an interpretable prediction.</h2>
        </div>
        <ol class="plant-flow" aria-label="MRI research workflow">
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2h8l4 4v16H6V2Z"/><path d="M14 2v5h5M9 11h6M9 15h6M9 19h4"/></svg>
            </span>
            <div><span>DATA</span><strong>Prepare the MRI dataset</strong><p>Combine public collections, remove unsuitable samples and separate training, validation and test data.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="16" rx="2"/><path d="M3 8h18M7 6h.01M10 6h.01M8.5 12.5 6 15l2.5 2.5M15.5 12.5 18 15l-2.5 2.5M13.5 11.5l-3 7"/></svg>
            </span>
            <div><span>MODEL</span><strong>Train through transfer learning</strong><p>Use controlled augmentation and adapt VGG16 visual features to distinguish the four MRI classes.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="13" rx="2"/><path d="M7 13V9M11 13V7M15 13v-3M19 13V6M9 21h6M12 17v4"/></svg>
            </span>
            <div><span>OUTPUT</span><strong>Evaluate and deliver predictions</strong><p>Assess every class and serve probabilities through a Flask application with numerical and visual results.</p></div>
          </li>
        </ol>
      </section>

      <section class="case-section visual-story" aria-labelledby="model-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">MODEL EVOLUTION</p>
            <h2 id="model-title">The first model revealed the problem. VGG16 made it scalable.</h2>
          </div>
          <div>
            <p>An initial CNN covering two classes established the preprocessing and evaluation workflow, but its small dataset produced unstable validation behaviour. Increasing and cleaning the data improved the experiment before expanding it to four classes.</p>
            <p>Transfer learning reused VGG16's pretrained visual features. Freezing most of the network and adapting the output reduced the trainable portion to roughly 16,000 parameters for the target classification task.</p>
          </div>
        </div>
        <figure class="research-figure wide-figure">
          <button type="button" class="figure-zoom-trigger" data-lightbox-trigger aria-label="Enlarge VGG16 architecture diagram">
            <img src="/images/projects/brain-tumour-mri/vgg16-architecture-final-lilac-20260829.png" alt="VGG16 transfer learning architecture used for MRI classification across four classes" />
            <span aria-hidden="true">Expand</span>
          </button>
          <figcaption>VGG16 transfer learning architecture adapted during the research.</figcaption>
        </figure>
        <ol class="plant-flow" aria-label="MRI model evolution">
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="5" cy="8" r="2"/><circle cx="5" cy="16" r="2"/><circle cx="12" cy="12" r="2"/><circle cx="19" cy="12" r="2"/><path d="m6.8 9 3.4 2M6.8 15l3.4-2M14 12h3"/></svg>
            </span>
            <div><span>INITIAL EXPERIMENT</span><strong>CNN baseline with two classes</strong><p>A custom network used 620 images to validate the workflow. Unstable validation exposed overfitting and the limits of a small dataset.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="5" width="5" height="14" rx="1"/><rect x="10" y="7" width="5" height="10" rx="1"/><rect x="17" y="9" width="4" height="6" rx="1"/><path d="M8 12h2M15 12h2"/></svg>
            </span>
            <div><span>FINAL EXPERIMENT</span><strong>Transfer learning across four classes</strong><p>The study expanded to 5,320 balanced images. VGG16 transfer learning reduced the trainable output to roughly 16,000 parameters.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="5" width="14" height="14" rx="2"/><path d="M2 8V2h6M16 2h6v6M22 16v6h-6M8 22H2v-6M9 12h6M12 9v6"/></svg>
            </span>
            <div><span>INPUT SHAPE</span><strong>224 × 224 × 3</strong><p>Every image was resized into a consistent three channel tensor, making heterogeneous source resolutions compatible with one feature extraction pipeline.</p></div>
          </li>
        </ol>
      </section>

      <section class="case-section" aria-labelledby="data-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">DATA STRATEGY</p>
            <h2 id="data-title">More data helped. Better controlled data mattered more.</h2>
          </div>
          <div><p>The final experiment used 5,320 MRI images with balanced class counts. A 70/20/10 strategy separated learning, model selection and final evaluation, while augmentation introduced controlled rotations, shifts, zoom and flips exclusively during training.</p></div>
        </div>
        <div class="dataset-bar" aria-label="Dataset distribution">
          <div class="train"><strong>3,720</strong><span>Training · 70%</span></div>
          <div class="validation"><strong>1,080</strong><span>Validation · 20%</span></div>
          <div class="test"><strong>520</strong><span>Test · 10%</span></div>
        </div>
        <figure class="research-figure">
          <button type="button" class="figure-zoom-trigger" data-lightbox-trigger aria-label="Enlarge MRI data augmentation examples">
            <img src="/images/projects/brain-tumour-mri/data-augmentation.png" alt="MRI image and augmented variants used during model training" />
            <span aria-hidden="true">Expand</span>
          </button>
          <figcaption>Controlled augmentation generated new training variants while preserving the original anatomy.</figcaption>
        </figure>
        <section class="dataset-sources" aria-labelledby="sources-title">
          <div>
            <p class="case-eyebrow">OPEN ACADEMIC DATA</p>
            <h3 id="sources-title">Where the MRI images came from.</h3>
            <p>The research combined publicly accessible MRI collections used for academic experimentation. Images from the different sources were reviewed, reorganised and filtered before entering the common training workflow.</p>
          </div>
          <div class="source-links">
            <a class="interactive-control neon-box-control premium-neon-control dataset-source-card" href="https://figshare.com/articles/dataset/brain_tumor_dataset/1512427" target="_blank" rel="noreferrer">
              <span>FIGSHARE</span><strong>Brain Tumor Dataset</strong><small>3,064 T1 weighted MRI images with contrast enhancement across three tumour classes. CC BY 4.0.</small>
              <span class="dataset-source-arrow neon-box-arrow" aria-hidden="true">↗</span>
            </a>
            <a class="interactive-control neon-box-control premium-neon-control dataset-source-card" href="https://www.kaggle.com/datasets/sartajbhuvaji/brain-tumor-classification-mri" target="_blank" rel="noreferrer">
              <span>KAGGLE</span><strong>Brain Tumor Classification (MRI)</strong><small>Public academic collection organised around glioma, meningioma, pituitary and images without a tumour.</small>
              <span class="dataset-source-arrow neon-box-arrow" aria-hidden="true">↗</span>
            </a>
            <a class="interactive-control neon-box-control premium-neon-control dataset-source-card" href="https://github.com/MohamedAliHabib/Brain-Tumor-Detection" target="_blank" rel="noreferrer">
              <span>GITHUB</span><strong>Brain Tumor Detection Dataset</strong><small>Open reference repository containing MRI samples and CNN experimentation material.</small>
              <span class="dataset-source-arrow neon-box-arrow" aria-hidden="true">↗</span>
            </a>
          </div>
        </section>
      </section>

      <section class="case-section" aria-labelledby="results-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">EXPERIMENTAL RESULTS</p>
          <h2 id="results-title">Strong results across classes, with visible room for improvement.</h2>
          <p>The model covering four classes reached 88.17% training accuracy. Evaluation of each class showed that glioma remained the most difficult category, reinforcing the effect of visual similarity and dataset quality.</p>
        </div>
        <figure class="benchmark-figure">
          <div class="benchmark-chart" role="group" aria-label="Comparison between target class reference results and the predicted label performance of the applied model">
            <section class="performance-card" aria-labelledby="comparison-chart-title">
              <div class="benchmark-panel-heading">
                <span>COMPARISON BY CLASS</span>
                <strong id="comparison-chart-title">Target benchmark compared with predicted label accuracy</strong>
                <div class="chart-legend" aria-label="Chart legend"><span><i class="target-key"></i>Target benchmark</span><span><i class="predicted-key"></i>Predicted label</span></div>
              </div>
              <div class="comparison-plot">
                {[
                  { label: "Meningioma", target: 74.0, predicted: 95.38 },
                  { label: "Glioma", target: 85.7, predicted: 81.54 },
                  { label: "Pituitary", target: 88.4, predicted: 87.66 },
                  { label: "No tumour", target: null, predicted: 93.08 },
                ].map(({ label, target, predicted }) => (
                  <div class:list={["comparison-group", target === null && "has-no-target"]} aria-label={`${label}: ${target === null ? "target benchmark not available" : `target ${target.toFixed(2)} percent`}; predicted ${predicted.toFixed(2)} percent`}>
                    <div class="comparison-row-heading">
                      <strong class="comparison-class">{label}</strong>
                      {target !== null
                        ? <span class:list={["comparison-delta", predicted >= target ? "is-positive" : "is-negative"]}>{predicted >= target ? "+" : ""}{(predicted - target).toFixed(2)} pp</span>
                        : <span class="comparison-delta is-unavailable">Benchmark not available</span>}
                    </div>
                    <div class="dumbbell-row">
                      <span class="dumbbell-value target-value">{target !== null ? `${target.toFixed(2)}%` : "N/A"}<small>Target</small></span>
                      <div
                        class:list={["dumbbell-track", target === null && "is-unavailable"]}
                        style={`--target:${target ?? predicted}%;--predicted:${predicted}%;--range-start:${Math.min(target ?? predicted, predicted)}%;--range-width:${Math.abs(predicted - (target ?? predicted))}%`}
                        aria-hidden="true"
                      >
                        <span class="dumbbell-axis"></span>
                        {target !== null && <span class="dumbbell-connector"></span>}
                        {target !== null && <span class="dumbbell-dot target-dot"></span>}
                        <span class="dumbbell-dot predicted-dot"></span>
                      </div>
                      <span class="dumbbell-value predicted-value">{predicted.toFixed(2)}%<small>Predicted</small></span>
                    </div>
                  </div>
                ))}
                <div class="comparison-scale" aria-hidden="true"><div class="comparison-axis"><span>0</span><span>25</span><span>50</span><span>75</span><span>100</span></div><strong>Accuracy (%)</strong></div>
              </div>
            </section>
          </div>
          <figcaption>
            <strong>Impact of the applied techniques.</strong>
            <span>The model exceeded the reference result for meningioma, remained close for pituitary images and showed a measurable gap for glioma. The no-tumour class reached 93.08%, although no equivalent benchmark was available for a direct comparison.</span>
          </figcaption>
        </figure>
      </section>

      <section class="case-section application-section" aria-labelledby="application-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">FROM MODEL TO PRODUCT</p>
            <h2 id="application-title">From trained model to functional application.</h2>
          </div>
          <div>
            <p>As part of the study, I developed a web application that connected the trained Keras model to a Python and Flask backend. Users could submit an MRI image and receive probabilities across four classes, presented numerically and through D3.js visualisations, demonstrating how the research model could operate within a practical workflow for users.</p>
            <p class="prediction-study-copy">To demonstrate the complete inference workflow, a pituitary MRI sample was submitted to the application. The report records the selected image, the probability assigned to each class and the final classification returned by the model.</p>
          </div>
        </div>
        <figure class="prediction-report">
          <button type="button" class="figure-zoom-trigger prediction-report-image" data-lightbox-trigger aria-label="Enlarge pituitary tumour MRI input">
            <img src="/images/projects/brain-tumour-mri/class-pituitary.png" alt="Pituitary tumour MRI selected as input for prediction across four classes" />
            <span aria-hidden="true">Expand</span>
          </button>
          <pre aria-label="Prediction report covering four classes">========================================
MRI CLASSIFICATION REPORT
========================================

INPUT DATA
FILE............. pituitary (654).png
INPUT TYPE....... MRI IMAGE
MODEL OUTPUT..... FOUR CLASSES

PREDICTION SCORES
MENINGIOMA....... 0.007176
NO TUMOUR........ 0.000283
GLIOMA........... 0.051178
PITUITARY........ 0.941363

PRIMARY CLASSIFICATION
LABEL............ PITUITARY
CONFIDENCE....... 94.1363%

REPORT STATUS.... COMPLETE
END OF REPORT
========================================</pre>
          <figcaption>Pituitary MRI input and the four probability values returned by the trained model.</figcaption>
        </figure>
      </section>

      <section class="case-section responsible-use-section" aria-labelledby="responsible-use-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">RESPONSIBLE USE</p>
          <h2 id="responsible-use-title">Responsible use boundary.</h2>
          <p>This research prototype supports experimentation and cannot replace diagnosis by qualified medical professionals.</p>
        </div>
        <a href="/documents/master-thesis-brain-tumour-mri.pdf" class="interactive-control neon-box-control premium-neon-control report-access-control" data-report-open>
          <span class="neon-box-label">Explore report</span>
        </a>
      </section>

      <section class="case-section technology-section" aria-labelledby="technology-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">TECHNICAL FOUNDATION</p>
          <h2 id="technology-title">Tools behind the research.</h2>
          <p>The stack connected model development, accelerated experimentation, backend inference and visualisation in the browser within one complete workflow.</p>
        </div>
        <div class="case-tags" aria-label="Core technologies">
          {project.technicalScope.map((item) => <span>{item}</span>)}
        </div>
      </section>

      <footer class="case-footer">
        <div class="research-references">
          <span>RESEARCH INTEGRITY</span>
          <a href="https://www.allea.org/wp-content/uploads/2017/05/ALLEA-European-Code-of-Conduct-for-Research-Integrity-2017.pdf" target="_blank" rel="noopener noreferrer">
            <img src="/images/allea-logo-lilac.png" alt="ALLEA" />
            <span>The European Code of Conduct for Research Integrity</span>
          </a>
        </div>
      </footer>

      <dialog class="figure-lightbox" data-figure-lightbox aria-label="Expanded VGG16 architecture diagram">
        <div class="figure-lightbox-content">
          <button type="button" class="figure-lightbox-close" data-figure-close aria-label="Close enlarged image">×</button>
          <img data-lightbox-image src="/images/projects/brain-tumour-mri/vgg16-architecture-final-lilac-20260829.png" alt="Expanded research figure" />
        </div>
      </dialog>

      <dialog class="report-viewer" data-report-viewer aria-label="Academic research report">
        <div class="report-viewer-content">
          <button type="button" class="report-viewer-close" data-report-close aria-label="Close research report">
            <svg aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
              <path d="M6 6l12 12M18 6L6 18" />
            </svg>
          </button>
          <iframe title="Academic research report" data-report-frame data-source="/documents/master-thesis-brain-tumour-mri.pdf#toolbar=0&navpanes=0&scrollbar=1&view=FitH"></iframe>
        </div>
      </dialog>
    </main>
  ) : isGridStabilityCaseStudy ? (
    <main class="case-study grid-case-study">
      <section class="case-hero">
        <FloatingBackControl href="/#projects" label="Back to projects" />

        <div class="case-hero-grid">
          <div class="case-hero-copy">
            <p class="case-eyebrow">PROFESSIONAL CONTRIBUTION · POWER SYSTEMS</p>
            <h1>Transmission grid stability decision support.</h1>
            <p class="case-lead">Collaborative software engineering for applications that evaluate controlled network contingencies and help specialists understand transmission system behaviour.</p>
          </div>

          <figure class="grid-hero-visual">
            <button type="button" class="figure-zoom-trigger grid-architecture-trigger" data-lightbox-trigger aria-label="Enlarge conceptual transmission stability system architecture">
              <img src="/images/projects/grid-stability/system-architecture.png" alt="Conceptual architecture linking the power system, PMU devices, a VAMS server, database, stability assessment model, visualisation and operators" />
              <span aria-hidden="true">Expand</span>
            </button>
            <figcaption>Conceptual system architecture reconstructed for this portfolio from the public engineering workflow.</figcaption>
          </figure>
        </div>
      </section>

      <section class="case-section" aria-labelledby="grid-context-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">OPERATIONAL CONTEXT</p>
            <h2 id="grid-context-title">Supporting stability across a complex transmission environment.</h2>
          </div>
          <div>
            <p>The project supports one of Europe’s leading electricity transmission operators, responsible for the Dutch high voltage grid and a substantial part of the German network.</p>
            <p>As renewable generation, interconnection and operational complexity increase, engineering teams need dependable applications that convert network conditions into structured simulations and reviewable technical results.</p>
          </div>
        </div>
        <div class="network-context-layout">
          <figure class="network-context-map">
            <button type="button" class="figure-zoom-trigger" data-lightbox-trigger aria-label="Enlarge conceptual map of the Dutch transmission grid and North Sea connections">
              <img src="/images/projects/grid-stability/dutch-grid-context.png" alt="Conceptual map highlighting the Netherlands, its transmission network, North Sea wind areas, energy hubs and regional corridors" />
              <span aria-hidden="true">Expand</span>
            </button>
            <figcaption>Conceptual reconstruction of the regional grid vision, centred on the Netherlands.</figcaption>
          </figure>
          <div class="network-context-metrics" aria-label="Future electricity system planning context">
            <article><span>PLANNING HORIZON</span><strong>2045</strong><p>A long term grid vision connecting offshore generation, energy hubs and reinforced continental infrastructure.</p></article>
            <article><span>ELECTRICITY DEMAND</span><strong>More than 2x</strong><p>Projected consumption could more than double across the Dutch and German energy systems.</p></article>
            <article><span>GENERATION CAPACITY</span><strong>5x to 10x</strong><p>Future scenarios anticipate a major expansion of generation capacity as electrification accelerates.</p></article>
            <article><span>OFFSHORE WIND</span><strong>About 70 GW</strong><p>The planning context considers approximately 70 GW of offshore wind for each country.</p></article>
            <p class="source-note">Future planning context: <a href="https://www.offshorewind.biz/2023/04/13/tennet-unveils-electricity-grid-plans-for-2045/" target="_blank" rel="noopener noreferrer">Electricity grid plans for 2045</a>. The map is a conceptual portfolio reconstruction, not an operational network diagram.</p>
          </div>
        </div>
      </section>

      <section class="case-section" aria-labelledby="contingency-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">CONTINGENCY DRIVEN ANALYSIS</p>
          <h2 id="contingency-title">Controlled disturbances reveal how the system responds.</h2>
          <p>A contingency represents a defined change introduced into a network simulation. Rather than predicting an incident in isolation, the application evaluates the resulting system response under selected operating conditions.</p>
        </div>
        <div class="contingency-grid">
          <article><span>LINE EVENT</span><strong>Transmission path unavailable</strong><p>A conceptual line outage tests how power flows redistribute through the remaining network.</p></article>
          <article><span>GENERATION EVENT</span><strong>Sudden production loss</strong><p>A controlled generation loss helps reveal frequency and stability behaviour after an imbalance.</p></article>
          <article><span>NETWORK EVENT</span><strong>Temporary electrical fault</strong><p>A simulated disturbance allows specialists to examine whether the system returns to an acceptable state.</p></article>
        </div>
        <p class="concept-note">The examples are general engineering scenarios and do not reproduce operational configurations or internal assessment criteria.</p>
      </section>

      <section class="case-section" aria-labelledby="grid-flow-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">ANALYSIS WORKFLOW</p>
          <h2 id="grid-flow-title">From operational context to reviewable evidence.</h2>
        </div>
        <ol class="plant-flow" aria-label="Grid stability analysis workflow">
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><ellipse cx="12" cy="5" rx="7" ry="3"/><path d="M5 5v6c0 1.7 3.1 3 7 3s7-1.3 7-3V5M5 11v6c0 1.7 3.1 3 7 3s7-1.3 7-3v-6"/></svg>
            </span>
            <div><span>INPUT</span><strong>Prepare the operating scenario</strong><p>Transform validated network information and a selected contingency into consistent simulation inputs.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="13" rx="2"/><path d="m7 11 2.2 2.2L13.5 9M9 21h6M12 17v4"/></svg>
            </span>
            <div><span>SIMULATION</span><strong>Evaluate system response</strong><p>Coordinate specialist tools that calculate behaviour during and after the controlled disturbance.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="13" rx="2"/><path d="m7 13 3-3 2.5 2.5L17 8M14 8h3v3M9 21h6M12 17v4"/></svg>
            </span>
            <div><span>REVIEW</span><strong>Deliver traceable results</strong><p>Present structured outputs that specialists can verify, compare and use within established decisions.</p></div>
          </li>
        </ol>
      </section>

      <section class="case-section" aria-labelledby="contribution-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">ENGINEERING CONTRIBUTION</p>
            <h2 id="contribution-title">Software built within a multidisciplinary delivery environment.</h2>
          </div>
          <div>
            <p>My contribution covers the design, development, testing and deployment of application components across offline and online environments. The work connects software engineering with power system analysis, data preparation and operational requirements.</p>
            <p>I also support integration with existing infrastructure and specialist simulation tools, validating datasets, inputs and outputs so that results remain accurate, reproducible and suitable for technical review.</p>
          </div>
        </div>
      </section>

      <section class="case-section crof-section" aria-labelledby="crof-title">
        <div class="section-heading compact-heading crof-heading">
          <p class="case-eyebrow">CONTROL ROOM OF THE FUTURE</p>
          <h2 id="crof-title">A more integrated environment for operational decision support.</h2>
          <p>CRoF brings grid data, network models, security assessment and decision support into a coordinated environment. Its purpose is to combine automation with human supervision as system complexity grows.</p>
        </div>
        <div class="crof-gallery">
          <figure>
            <button type="button" class="figure-zoom-trigger" data-lightbox-trigger aria-label="Enlarge future electricity control room view">
              <img src="/images/projects/grid-stability/control-room-future.png" alt="Three operators reviewing network diagrams and operational displays in an integrated electricity control room" />
              <span aria-hidden="true">Expand</span>
            </button>
            <figcaption>Integrated monitoring places network behaviour and operational evidence within one review environment.</figcaption>
          </figure>
          <figure>
            <button type="button" class="figure-zoom-trigger" data-lightbox-trigger aria-label="Enlarge grid management facility view">
              <img src="/images/projects/grid-stability/grid-management-facility.png" alt="Isometric engineering view of a secured grid management facility and high voltage infrastructure" />
              <span aria-hidden="true">Expand</span>
            </button>
            <figcaption>Operational software remains connected to the physical infrastructure it helps specialists understand.</figcaption>
          </figure>
        </div>
      </section>

      <section class="case-section literature-section" aria-labelledby="literature-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">RELATED RESEARCH</p>
            <h2 id="literature-title">Online assessment connects system behaviour with visual decision support.</h2>
          </div>
          <div>
            <p>Published research also explores data driven online assessment combined with spatial and temporal visualisation. This reinforces a central principle of the project: simulation output creates greater value when specialists can interpret system behaviour through structured and accessible evidence.</p>
            <a class="interactive-control neon-box-control premium-neon-control literature-link" href="https://doi.org/10.1016/j.ijepes.2020.106316" target="_blank" rel="noopener noreferrer">
              <span class="neon-box-label">Explore related paper</span>
            </a>
          </div>
        </div>
      </section>

      <section class="case-section" aria-labelledby="reflection-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">PROFESSIONAL REFLECTION</p>
          <h2 id="reflection-title">Engineering value emerges through collaboration and disciplined integration.</h2>
        </div>
        <ol class="plant-flow grid-reflection" aria-label="Professional contribution and learning">
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="3" width="16" height="18" rx="2"/><path d="M4 9h16M4 15h16"/><circle cx="8" cy="6" r="0.8" fill="currentColor" stroke="none"/><circle cx="8" cy="12" r="0.8" fill="currentColor" stroke="none"/><circle cx="8" cy="18" r="0.8" fill="currentColor" stroke="none"/><path d="M12 6h5M12 12h5M12 18h5"/></svg>
            </span>
            <div><span>WHAT I CONTRIBUTED</span><strong>Software and integration</strong><p>Application development, deployment support and validation across interconnected engineering environments.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2h8l4 4v16H6V2Z"/><path d="M14 2v5h5M9 11h6M9 15h6M9 19h4"/></svg>
            </span>
            <div><span>WHAT MADE IT COMPLEX</span><strong>Operational context</strong><p>Every technical result depends on consistent data, specialist tools and coordinated interpretation across disciplines.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 7h8M6 7V6a3 3 0 0 1 6 0v1"/><circle cx="9" cy="9.5" r="2.2"/><path d="M6.5 21v-6.5A2.5 2.5 0 0 1 9 12h1.2c1.4 0 2.3.7 3.2 1.6l1.1 1.1M9.5 21v-5M13.4 13.6l3.1-2.6"/><path d="M15.5 8.5 18 6l2 1.5 2-3M16 11h6"/></svg>
            </span>
            <div><span>WHAT IT STRENGTHENED</span><strong>Systems thinking</strong><p>The work expanded my ability to connect software decisions with power system behaviour and operational reliability.</p></div>
          </li>
        </ol>
      </section>

      <section class="case-section technology-section" aria-labelledby="grid-technology-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">TECHNICAL FOUNDATION</p>
          <h2 id="grid-technology-title">Tools supporting the engineering workflow.</h2>
          <p>The technical environment combines Python application development with PowerTech DSA Tools, containerised services, infrastructure integration, simulation data and deployment across multiple operating systems.</p>
        </div>
        <div class="case-tags" aria-label="Core technologies">
          {project.technicalScope.map((item) => <span>{item}</span>)}
        </div>
      </section>

      <dialog class="figure-lightbox" data-figure-lightbox aria-label="Expanded conceptual transmission stability system architecture">
        <div class="figure-lightbox-content">
          <button type="button" class="figure-lightbox-close" data-figure-close aria-label="Close enlarged image">×</button>
          <img data-lightbox-image src="/images/projects/grid-stability/system-architecture.png" alt="Expanded conceptual transmission stability system architecture" />
        </div>
      </dialog>
    </main>
  ) : isRenewableControlCaseStudy ? (
    <main class="case-study renewable-case-study">
      <section class="case-hero renewable-hero">
        <FloatingBackControl href="/#projects" label="Back to projects" />

        <div class="case-hero-grid">
          <div class="case-hero-copy">
            <p class="case-eyebrow">PROFESSIONAL CONTRIBUTION · RENEWABLE CONTROL</p>
            <h1>Coordinating utility scale photovoltaic power.</h1>
            <p class="case-lead">Control and systems integration for a 380 MW photovoltaic plant in Extremadura, Spain, connecting PPC, PLC, SCADA and HMI layers with the operational behaviour expected at the grid connection point.</p>
          </div>

          <figure class="renewable-hero-visual">
            <button type="button" class="figure-zoom-trigger" data-lightbox-trigger aria-label="Enlarge conceptual view of the photovoltaic plant">
              <img src="/images/projects/renewable-control/extremadura-solar-control-plant.png" alt="Aerial reconstruction of a utility scale photovoltaic plant and its control and electrical infrastructure in Extremadura" />
              <span aria-hidden="true">Expand</span>
            </button>
            <figcaption>Conceptual reconstruction of the photovoltaic field, control building and electrical infrastructure.</figcaption>
          </figure>
        </div>
      </section>

      <section class="case-section" aria-labelledby="solar-context-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">OPERATIONAL CONTEXT</p>
            <h2 id="solar-context-title">A large plant within Spain's photovoltaic transformation.</h2>
          </div>
          <div>
            <p>Extremadura has become one of Spain's most important solar regions. The plant context used for this portfolio covers 380 MW of capacity, more than 930,000 fixed structure panels and 226 inverters distributed across a vast generation site.</p>
            <p>That scale changes the control problem. Thousands of field devices must respond as one coordinated power plant, with reliable supervision and a predictable response at the grid connection point.</p>
          </div>
        </div>
      </section>

      <section class="case-section" aria-labelledby="control-architecture-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">CONTROL ARCHITECTURE</p>
          <h2 id="control-architecture-title">From solar field to grid response.</h2>
          <p>Field information moves through deterministic automation and plant level regulation before operators supervise the coordinated response at the connection point.</p>
        </div>
        <ol class="plant-flow" aria-label="Conceptual photovoltaic plant control flow">
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="m4 7 16-2 1 11-16 2L4 7Z"/><path d="m9.3 6.35 1 10.95M14.65 5.7l1 10.95M4.5 11.25l16-2M4.9 15l16-2"/><path d="M12 17.15V21M8.5 21h7"/></svg>
            </span>
            <div><span>FIELD ACQUISITION</span><strong>Panels, inverters and sensors</strong><p>Measurements and equipment states establish the live condition of the solar field.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="4" y="4" width="16" height="16" rx="2"/><path d="M8 9h8M8 15h8M9 4V2M15 4V2M9 22v-2M15 22v-2M4 9H2M4 15H2M22 9h-2M22 15h-2"/></svg>
            </span>
            <div><span>COORDINATED CONTROL</span><strong>PLC and Power Plant Controller</strong><p>Deterministic logic and plant regulation convert grid targets into coordinated commands.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="13" rx="2"/><path d="m7 12 2.3-2.4 2.2 1.9 3.2-3.2L17 10.5M9 21h6M12 17v4"/></svg>
            </span>
            <div><span>SUPERVISED RESPONSE</span><strong>SCADA, HMI and grid connection</strong><p>Operators verify plant behaviour while the installation responds as one generation resource.</p></div>
          </li>
        </ol>
      </section>

      <section class="case-section" aria-labelledby="engineering-work-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">ENGINEERING CONTRIBUTION</p>
            <h2 id="engineering-work-title">Making the control layers work together on site.</h2>
          </div>
          <div>
            <p>My work supported the deployment and commissioning of a PLC based Power Plant Controller, including its integration with the HMI, SCADA environment and plant communications.</p>
            <p>The role connected control logic with real installation conditions through structured testing, technical troubleshooting and coordination during commissioning, while validating the plant response against defined grid requirements.</p>
          </div>
        </div>
        <ol class="plant-flow" aria-label="Engineering contribution workflow">
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="3" width="16" height="18" rx="2"/><path d="M4 9h16M4 15h16"/><circle cx="8" cy="6" r="0.8" fill="currentColor" stroke="none"/><circle cx="8" cy="12" r="0.8" fill="currentColor" stroke="none"/><circle cx="8" cy="18" r="0.8" fill="currentColor" stroke="none"/><path d="M12 6h5M12 12h5M12 18h5"/></svg>
            </span>
            <div><span>CONTROL</span><strong>Coordinate plant behaviour</strong><p>Support PPC and PLC logic that converts grid level objectives into consistent commands across distributed equipment.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="13" rx="2"/><path d="M7 9h4M7 12h7M12 17v4M8.5 21h7"/><circle cx="17.5" cy="9" r="1" fill="currentColor" stroke="none"/></svg>
            </span>
            <div><span>INTEGRATION</span><strong>Connect automation and supervision</strong><p>Bring SCADA, HMI, controllers and communications into a coherent operational workflow.</p></div>
          </li>
          <li>
            <span class="plant-flow-symbol" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3 4.5 6v5.5c0 4.7 3.2 7.8 7.5 9.5 4.3-1.7 7.5-4.8 7.5-9.5V6L12 3Z"/><path d="m8.5 12 2.2 2.2 4.8-5"/></svg>
            </span>
            <div><span>VALIDATION</span><strong>Test the complete response</strong><p>Verify signals, sequences, redundancy and plant behaviour under representative commissioning conditions.</p></div>
          </li>
        </ol>
      </section>

      <section class="case-section control-system-section" aria-labelledby="control-system-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">INDUSTRIAL TECHNOLOGY</p>
            <h2 id="control-system-title">Software decisions connected to physical equipment.</h2>
          </div>
          <div>
            <p>PLC hardware executes dependable control close to the process. The PPC coordinates active and reactive behaviour at plant level, while SCADA and HMI systems expose status, alarms and trends to engineering and operations teams.</p>
            <p>Industrial communications and redundant services keep those layers connected, making signal quality, time consistency and failover behaviour as important as the application logic itself.</p>
          </div>
        </div>
        <figure class="renewable-control-visual">
          <button type="button" class="figure-zoom-trigger" data-lightbox-trigger aria-label="Enlarge conceptual hybrid renewable plant control architecture">
            <picture>
              <source media="(max-width: 640px)" srcset="/images/projects/renewable-control/hybrid-control-architecture-mobile-v4.svg?v=6" />
              <img src="/images/projects/renewable-control/hybrid-control-architecture-v4.svg?v=6" alt="Integrated hybrid renewable architecture connecting a three-turbine wind farm and solar generation through local PPCs, hybrid plant coordination, operator supervision and the electrical grid point of interconnection" />
            </picture>
            <span aria-hidden="true">Expand</span>
          </button>
          <figcaption>Conceptual hybrid control architecture reconstructed for this portfolio. It does not reproduce proprietary topology, interfaces or operational settings.</figcaption>
        </figure>
      </section>

      <section class="case-section" aria-labelledby="solar-transition-title">
        <div class="split-copy">
          <div>
            <p class="case-eyebrow">ENERGY TRANSITION</p>
            <h2 id="solar-transition-title">Photovoltaic growth increases the value of dependable control.</h2>
          </div>
          <div>
            <p>Spain's photovoltaic fleet expanded rapidly during the last decade. By early 2025, solar PV had become the country's largest generation technology by installed capacity, reaching 32,043 MW and almost one quarter of the national generation fleet.</p>
            <p>As more solar capacity connects to the system, plant controllers become essential infrastructure: they help large installations translate variable generation into a measurable, supervised and coordinated grid response.</p>
            <a class="interactive-control neon-box-control premium-neon-control literature-link" href="https://www.ree.es/en/press-office/press-release/news/press-release/2025/02/solar-pv-takes-lead-spains-installed-power-capacity" target="_blank" rel="noopener noreferrer">
              <span class="neon-box-label">Explore national context</span>
            </a>
          </div>
        </div>
      </section>

      <section class="case-section responsible-use-section" aria-labelledby="renewable-boundary-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">PORTFOLIO BOUNDARY</p>
          <h2 id="renewable-boundary-title">Experience presented without proprietary detail.</h2>
          <p>The descriptions and visuals communicate my engineering contribution at a systems level. They do not disclose plant setpoints, internal topology, source code, operational procedures or confidential interfaces.</p>
        </div>
      </section>

      <section class="case-section technology-section" aria-labelledby="renewable-technology-title">
        <div class="section-heading compact-heading">
          <p class="case-eyebrow">TECHNICAL FOUNDATION</p>
          <h2 id="renewable-technology-title">Tools supporting the plant control workflow.</h2>
          <p>The environment combined plant level regulation, deterministic automation, operator supervision, industrial communications, redundancy and structured commissioning.</p>
        </div>
        <div class="case-tags" aria-label="Core technologies">
          {project.technicalScope.map((item) => <span>{item}</span>)}
        </div>
      </section>

      <dialog class="figure-lightbox" data-figure-lightbox aria-label="Expanded photovoltaic plant case study image">
        <div class="figure-lightbox-content">
          <button type="button" class="figure-lightbox-close" data-figure-close aria-label="Close enlarged image">×</button>
          <img data-lightbox-image src="/images/projects/renewable-control/extremadura-solar-control-plant.png" alt="Expanded photovoltaic plant and control infrastructure visual" />
        </div>
      </dialog>
    </main>
  ) : (
    <section class="relative isolate min-h-[calc(100svh-5rem)] overflow-hidden px-6 py-10 sm:px-10 lg:px-16">
      <FloatingBackControl href="/#projects" label="Back to projects" />

      <article class="mx-auto mt-24 max-w-4xl">
        <p class="text-sm font-medium tracking-[0.22em] text-violet-300">PROJECT CASE STUDY</p>
        <p class="mt-5 text-sm text-slate-400">{project.location}</p>
        <h1 class="interactive-heading mt-4 text-4xl font-semibold tracking-tight text-slate-400 sm:text-6xl">{project.title}</h1>
        <p class="mt-8 max-w-3xl text-xl leading-9 text-slate-300">{project.summary}</p>

        <div class="mt-16 grid gap-12 border-t border-violet-300/15 pt-10 sm:grid-cols-2">
          <section>
            <p class="text-sm font-medium tracking-[0.18em] text-violet-300">CONTRIBUTION</p>
            <p class="mt-4 leading-8 text-slate-400">{project.contribution}</p>
          </section>
          <section>
            <p class="text-sm font-medium tracking-[0.18em] text-violet-300">OUTCOME</p>
            <p class="mt-4 leading-8 text-slate-400">{project.outcome}</p>
          </section>
        </div>

        <section class="mt-14 border-t border-violet-300/15 pt-10">
          <p class="text-sm font-medium tracking-[0.18em] text-violet-300">TECHNICAL SCOPE</p>
          <div class="mt-5 flex flex-wrap gap-3">
            {project.technicalScope.map((item) => <span class="tech-scope-item">{item}</span>)}
          </div>
        </section>
      </article>
    </section>
  )}
</BaseLayout>

<script>
  const zoomTriggers = document.querySelectorAll<HTMLButtonElement>("[data-lightbox-trigger]");
  const lightbox = document.querySelector<HTMLDialogElement>("[data-figure-lightbox]");
  const closeButton = document.querySelector<HTMLButtonElement>("[data-figure-close]");
  const lightboxImage = document.querySelector<HTMLImageElement>("[data-lightbox-image]");
  let activeTrigger: HTMLButtonElement | null = null;

  const closeLightbox = () => {
    if (!lightbox?.open) return;
    lightbox.close();
  };

  zoomTriggers.forEach((trigger) => {
    trigger.addEventListener("click", () => {
      const sourceImage = trigger.querySelector<HTMLImageElement>("img");
      if (!lightbox || !lightboxImage || !sourceImage) return;
      activeTrigger = trigger;
      lightboxImage.src = sourceImage.currentSrc || sourceImage.src;
      lightboxImage.alt = `Expanded ${sourceImage.alt}`;
      lightbox.setAttribute("aria-label", lightboxImage.alt);
      lightbox.showModal();
    });
  });
  closeButton?.addEventListener("click", closeLightbox);
  lightbox?.addEventListener("close", () => activeTrigger?.focus());
  lightbox?.addEventListener("click", (event) => {
    if (event.target === lightbox) closeLightbox();
  });

  const reportTrigger = document.querySelector<HTMLAnchorElement>("[data-report-open]");
  const reportViewer = document.querySelector<HTMLDialogElement>("[data-report-viewer]");
  const reportFrame = document.querySelector<HTMLIFrameElement>("[data-report-frame]");
  const reportClose = document.querySelector<HTMLButtonElement>("[data-report-close]");

  const closeReport = () => {
    if (!reportViewer?.open) return;
    reportViewer.close();
  };

  reportTrigger?.addEventListener("click", (event) => {
    if (!reportViewer || !reportFrame) return;
    event.preventDefault();
    reportFrame.src = reportFrame.dataset.source ?? reportTrigger.href;
    reportViewer.showModal();
  });
  reportClose?.addEventListener("click", closeReport);
  reportViewer?.addEventListener("click", (event) => {
    if (event.target === reportViewer) closeReport();
  });
  reportViewer?.addEventListener("close", () => {
    if (reportFrame) reportFrame.removeAttribute("src");
    reportTrigger?.focus();
  });

  if (document.querySelector(".case-study")) {
    let scrollEffectTimer: number | undefined;
    const updateScrollEffect = () => {
      document.documentElement.classList.add("case-is-scrolling");
      window.clearTimeout(scrollEffectTimer);
      scrollEffectTimer = window.setTimeout(() => document.documentElement.classList.remove("case-is-scrolling"), 150);
    };

    window.addEventListener("scroll", updateScrollEffect, { passive: true });
  }
</script>

<style>
  .case-study {
    --case-border: rgba(196, 181, 253, 0.22);
    --case-panel: rgba(13, 9, 22, 0.72);
    background:
      radial-gradient(circle at 82% 7%, rgba(124, 58, 237, 0.14), transparent 24rem),
      radial-gradient(circle at 12% 33%, rgba(34, 211, 238, 0.055), transparent 25rem),
      #000;
    color: rgb(203 213 225);
    overflow: hidden;
  }

  .case-hero,
  .case-section,
  .case-footer {
    margin-inline: auto;
    max-width: 86rem;
    padding-inline: clamp(1.35rem, 5vw, 5rem);
  }

  .case-hero {
    min-height: calc(100svh - 5rem);
    padding-bottom: clamp(5rem, 10vw, 9rem);
    padding-top: 2.5rem;
  }

  .case-hero-grid {
    align-items: center;
    display: grid;
    gap: clamp(3rem, 7vw, 8rem);
    grid-template-columns: minmax(0, 1.03fr) minmax(22rem, 0.97fr);
    min-height: calc(100svh - 12rem);
    padding-top: 2rem;
  }

  .case-eyebrow {
    color: rgb(196 181 253);
    font-size: 0.76rem;
    font-weight: 700;
    letter-spacing: 0.2em;
  }

  .case-hero h1 {
    color: rgb(226 232 240);
    font-size: clamp(2.1rem, 3.7vw, 3.6rem);
    font-weight: 620;
    letter-spacing: -0.055em;
    line-height: 0.94;
    margin-top: 1.4rem;
    max-width: 12ch;
    text-wrap: balance;
  }

  .case-lead {
    color: rgb(148 163 184);
    font-size: clamp(1.12rem, 1.65vw, 1.4rem);
    line-height: 1.75;
    margin-top: 2rem;
    max-width: 40rem;
  }

  .case-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
    margin-top: 2rem;
  }

  .case-tags span {
    border: 1px solid rgba(196, 181, 253, 0.23);
    border-radius: 999px;
    color: rgb(196 181 253);
    font-size: 0.72rem;
    font-weight: 650;
    letter-spacing: 0.08em;
    padding: 0.52rem 0.72rem;
    text-transform: uppercase;
  }

  .hero-visual {
    margin: 0;
    position: relative;
  }

  .scan-frame {
    background: #030305;
    border: 1px solid rgba(196, 181, 253, 0.32);
    border-radius: 1.6rem;
    box-shadow: 0 0 4rem rgba(124, 58, 237, 0.12);
    overflow: hidden;
    padding: clamp(0.8rem, 2vw, 1.5rem);
    position: relative;
  }

  .scan-frame::after {
    background: linear-gradient(135deg, transparent 42%, rgba(167, 139, 250, 0.11));
    content: "";
    inset: 0;
    pointer-events: none;
    position: absolute;
  }

  .scan-frame img {
    aspect-ratio: 1.12;
    filter: grayscale(1) contrast(1.18) hue-rotate(225deg);
    height: 100%;
    object-fit: cover;
    width: 100%;
  }

  .scan-line {
    animation: scan 5s ease-in-out infinite;
    background: linear-gradient(90deg, transparent, rgba(196, 181, 253, 0.8), transparent);
    box-shadow: 0 0 1rem rgba(167, 139, 250, 0.5);
    height: 1px;
    left: 1rem;
    position: absolute;
    pointer-events: none;
    right: 1rem;
    top: 18%;
    z-index: 2;
  }

  @keyframes scan { 0%, 100% { transform: translateY(0); opacity: 0.35; } 50% { transform: translateY(20rem); opacity: 0.9; } }

  .hero-visual figcaption,
  .research-figure figcaption {
    color: rgb(100 116 139);
    font-size: 0.75rem;
    letter-spacing: 0.04em;
    margin-top: 0.85rem;
  }

  .case-section {
    padding-bottom: clamp(3rem, 4vw, 4.5rem);
    padding-top: clamp(3rem, 4vw, 4.5rem);
  }

  .section-heading {
    max-width: 55rem;
  }

  .section-heading h2,
  .split-copy h2 {
    color: rgb(226 232 240);
    font-size: clamp(2.1rem, 3.7vw, 3.6rem);
    font-weight: 600;
    letter-spacing: -0.045em;
    line-height: 1.05;
    margin-top: 1.25rem;
    text-wrap: balance;
  }

  .section-heading > p:last-child,
  .split-copy > div:last-child p {
    color: rgb(148 163 184);
    font-size: 1.02rem;
    line-height: 1.85;
  }

  .section-heading > p:last-child { margin-top: 1.6rem; }
  .compact-heading { max-width: 64rem; }

  .metric-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin-top: 3.2rem;
  }

  .metric-grid article,
  .class-grid article {
    background: linear-gradient(145deg, rgba(17, 12, 28, 0.8), rgba(3, 3, 5, 0.65));
    border: 1px solid var(--case-border);
    border-radius: 1.1rem;
    padding: clamp(1.35rem, 3vw, 2.1rem);
  }

  .metric-grid article {
    align-content: center;
    min-height: 9.5rem;
    padding: clamp(1.1rem, 2vw, 1.45rem);
  }

  .metric-grid strong {
    color: rgb(196 181 253);
    display: block;
    font-size: clamp(2rem, 3.25vw, 3rem);
    letter-spacing: -0.05em;
  }

  .metric-grid span {
    color: rgb(148 163 184);
    display: block;
    line-height: 1.55;
    margin-top: 0.65rem;
  }

  .source-note {
    color: rgb(100 116 139);
    font-size: 0.78rem;
    line-height: 1.6;
    margin-top: 1.25rem;
  }

  .source-note a {
    color: rgb(196 181 253);
    text-decoration: underline;
    text-decoration-color: rgba(196, 181, 253, 0.38);
    text-underline-offset: 0.22em;
  }

  .split-copy {
    display: grid;
    gap: clamp(2rem, 7vw, 8rem);
    grid-template-columns: minmax(0, 0.95fr) minmax(0, 1.05fr);
  }

  .split-copy > div:last-child p + p { margin-top: 1.3rem; }

  .class-grid {
    display: grid;
    gap: 1rem;
    margin-top: 3.5rem;
  }

  .class-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); }

  .class-grid article {
    overflow: hidden;
    padding: 0;
  }

  .class-grid .class-image-trigger > img {
    aspect-ratio: 1;
    display: block;
    filter: grayscale(1) contrast(1.08);
    object-fit: cover;
    transition: filter 240ms ease, transform 350ms cubic-bezier(0.22, 1, 0.36, 1);
    width: 100%;
  }

  .class-grid article:hover .class-image-trigger > img,
  .class-grid .class-image-trigger:focus-visible > img {
    filter: grayscale(0.78) contrast(1.12) sepia(0.08) hue-rotate(220deg);
    transform: scale(1.025);
  }

  .class-grid article > div {
    border-top: 1px solid var(--case-border);
    min-height: 11rem;
    padding: 1.4rem;
    position: relative;
  }

  .class-grid h3 {
    color: rgb(196 181 253);
    font-size: 0.8rem;
    letter-spacing: 0.13em;
  }

  .class-grid p {
    color: rgb(148 163 184);
    font-size: 0.9rem;
    line-height: 1.65;
    margin-top: 0.75rem;
  }

  .research-figure {
    background: #050507;
    border: 1px solid var(--case-border);
    border-radius: 1.2rem;
    margin: 3rem 0 0;
    overflow: hidden;
    padding: clamp(0.75rem, 2vw, 1.4rem);
  }

  .research-figure img {
    background: #fff;
    border-radius: 0.7rem;
    display: block;
    height: auto;
    width: 100%;
  }

  .wide-figure img { filter: grayscale(0.25) saturate(0.75) hue-rotate(205deg); }

  .figure-zoom-trigger {
    background: transparent;
    border: 0;
    cursor: zoom-in;
    display: block;
    padding: 0;
    position: relative;
    width: 100%;
  }

  .figure-zoom-trigger::after {
    background: linear-gradient(135deg, transparent 45%, rgba(5, 5, 8, 0.86));
    border-radius: 0.7rem;
    content: "";
    inset: 0;
    opacity: 0;
    pointer-events: none;
    position: absolute;
    transition: opacity 180ms ease;
  }

  .figure-zoom-trigger > span {
    bottom: 1rem;
    color: rgb(221 214 254);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.13em;
    opacity: 0;
    position: absolute;
    right: 1rem;
    text-transform: uppercase;
    transition: opacity 180ms ease;
    z-index: 1;
  }

  .figure-zoom-trigger:hover::after,
  .figure-zoom-trigger:focus-visible::after,
  .figure-zoom-trigger:hover > span,
  .figure-zoom-trigger:focus-visible > span { opacity: 1; }

  .figure-zoom-trigger:focus-visible {
    border-radius: 0.7rem;
    outline: 2px solid rgb(196 181 253);
    outline-offset: 0.25rem;
  }

  .figure-lightbox {
    background: transparent;
    border: 0;
    height: 100dvh;
    margin: 0;
    max-height: none;
    max-width: none;
    padding: clamp(1rem, 4vw, 3rem);
    width: 100vw;
  }

  .figure-lightbox::backdrop {
    backdrop-filter: blur(12px);
    background: rgba(0, 0, 0, 0.84);
  }

  .figure-lightbox-content {
    align-items: center;
    display: flex;
    height: 100%;
    justify-content: center;
    margin: auto;
    max-width: 92rem;
    position: relative;
  }

  .figure-lightbox-content > img {
    background: #fff;
    border: 1px solid rgba(196, 181, 253, 0.42);
    border-radius: 0.9rem;
    box-shadow: 0 0 4rem rgba(124, 58, 237, 0.18);
    display: block;
    max-height: calc(100dvh - 5rem);
    max-width: 100%;
    object-fit: contain;
  }

  .figure-lightbox-close {
    align-items: center;
    background: rgba(8, 5, 14, 0.92);
    border: 1px solid rgba(196, 181, 253, 0.48);
    border-radius: 999px;
    color: rgb(237 233 254);
    cursor: pointer;
    display: flex;
    font-size: 1.5rem;
    height: 2.6rem;
    justify-content: center;
    line-height: 1;
    position: absolute;
    right: -0.8rem;
    top: 0;
    width: 2.6rem;
    z-index: 2;
  }

  .figure-lightbox-close:hover,
  .figure-lightbox-close:focus-visible {
    border-color: rgb(196 181 253);
    box-shadow: 0 0 1.2rem rgba(167, 139, 250, 0.3);
    outline: none;
  }

  .report-viewer {
    background: transparent;
    border: 0;
    height: 100dvh;
    margin: 0;
    max-height: none;
    max-width: none;
    padding: clamp(0.8rem, 3vw, 2rem);
    width: 100vw;
  }

  .report-viewer::backdrop {
    backdrop-filter: blur(12px);
    background: rgba(0, 0, 0, 0.88);
  }

  .report-viewer-content {
    background: #050507;
    border: 1px solid rgba(196, 181, 253, 0.36);
    border-radius: 1rem;
    box-shadow: 0 0 4rem rgba(124, 58, 237, 0.16);
    height: 100%;
    margin: 0 auto;
    max-width: 76rem;
    overflow: hidden;
    position: relative;
  }

  .report-viewer iframe {
    background: rgb(24 24 27);
    border: 0;
    height: 100%;
    width: 100%;
  }

  .report-viewer-close {
    align-items: center;
    background: rgba(8, 5, 14, 0.94);
    border: 1px solid rgba(196, 181, 253, 0.5);
    border-radius: 999px;
    color: rgb(226 232 240);
    cursor: pointer;
    display: inline-flex;
    height: 2.6rem;
    justify-content: center;
    position: absolute;
    right: 0.8rem;
    top: 0.8rem;
    width: 2.6rem;
    z-index: 2;
  }

  .report-viewer-close svg { height: 1.2rem; width: 1.2rem; }
  .report-viewer-close:hover,
  .report-viewer-close:focus-visible { border-color: rgb(196 181 253); box-shadow: 0 0 1.2rem rgba(167, 139, 250, 0.3); outline: none; }

  .dataset-sources {
    align-items: start;
    display: grid;
    gap: clamp(2rem, 7vw, 7rem);
    grid-template-columns: minmax(0, 0.75fr) minmax(0, 1.25fr);
    margin-top: clamp(4rem, 8vw, 7rem);
  }

  .dataset-sources h3 {
    color: rgb(226 232 240);
    font-size: clamp(1.7rem, 3vw, 2.6rem);
    font-weight: 600;
    letter-spacing: -0.035em;
    line-height: 1.1;
    margin-top: 1rem;
  }

  .dataset-sources > div:first-child > p:last-child {
    color: rgb(148 163 184);
    line-height: 1.8;
    margin-top: 1.2rem;
  }

  .source-links {
    display: grid;
    gap: 0.85rem;
  }

  .source-links a.dataset-source-card {
    align-content: center;
    display: grid;
    gap: 0.45rem;
    min-height: 8.5rem;
    padding: 1.35rem 3rem 1.35rem 1.4rem;
    position: relative;
  }

  .source-links .dataset-source-arrow {
    color: rgb(196 181 253);
    letter-spacing: 0;
    position: absolute;
    right: 1.25rem;
    top: 50%;
    transform: translateY(-50%);
    transition: color 180ms ease, transform 180ms ease;
  }

  .source-links a.dataset-source-card:hover .dataset-source-arrow,
  .source-links a.dataset-source-card:focus-visible .dataset-source-arrow {
    transform: translate(0.12rem, calc(-50% - 0.12rem));
  }

  .source-links a > span:first-child { color: rgb(139 92 246); font-size: 0.67rem; font-weight: 700; letter-spacing: 0.16em; }
  .source-links strong { color: rgb(226 232 240); font-size: 1rem; }
  .source-links small { color: rgb(100 116 139); line-height: 1.6; max-width: 44rem; }

  .dataset-bar {
    display: grid;
    gap: 0.25rem;
    grid-template-columns: 7fr 2fr 1fr;
    margin-top: 2.5rem;
    max-width: 68rem;
  }

  .dataset-bar > div {
    background: rgba(124, 58, 237, 0.32);
    border: 1px solid rgba(196, 181, 253, 0.28);
    min-height: 4.5rem;
    padding: 0.65rem 0.9rem;
  }

  .dataset-bar > div:first-child { border-radius: 1rem 0 0 1rem; }
  .dataset-bar > div:last-child { border-radius: 0 1rem 1rem 0; }
  .dataset-bar .validation { background: rgba(139, 92, 246, 0.21); }
  .dataset-bar .test { background: rgba(34, 211, 238, 0.11); }
  .dataset-bar strong { color: rgb(226 232 240); display: block; font-size: clamp(1.25rem, 2vw, 1.8rem); }
  .dataset-bar span { color: rgb(196 181 253); display: block; font-size: 0.68rem; margin-top: 0.35rem; }

  .benchmark-figure { margin: 4rem 0 0; max-width: 72rem; }

  .benchmark-chart {
    display: block;
  }

  .performance-card {
    background: linear-gradient(145deg, rgba(16, 11, 27, 0.66), rgba(3, 3, 5, 0.82));
    border: 1px solid var(--case-border);
    border-radius: 1.15rem;
    min-width: 0;
    padding: clamp(1.4rem, 3vw, 2.2rem);
  }

  .benchmark-panel-heading { border-bottom: 1px solid rgba(196, 181, 253, 0.15); margin-bottom: 1.8rem; padding-bottom: 1.2rem; }
  .benchmark-panel-heading > span { color: rgb(139 92 246); display: block; font-size: 0.67rem; font-weight: 700; letter-spacing: 0.16em; }
  .benchmark-panel-heading > strong { color: rgb(226 232 240); display: block; font-size: 1.1rem; margin-top: 0.45rem; }
  .chart-legend { display: flex; flex-wrap: wrap; gap: 1.2rem; margin-top: 1rem; }
  .chart-legend span { align-items: center; color: rgb(148 163 184); display: inline-flex; font-size: 0.72rem; gap: 0.45rem; letter-spacing: 0; }
  .chart-legend i { border-radius: 50%; display: inline-block; height: 0.55rem; width: 0.55rem; }
  .target-key { background: rgb(226 232 240); border: 1px solid rgb(226 232 240); }
  .predicted-key { background: rgb(139 92 246); border: 1px solid rgb(196 181 253); }
  .comparison-plot { --value-column: 5.4rem; }
  .comparison-scale { margin: 1.2rem var(--value-column) 0; }
  .comparison-scale > strong { color: rgb(196 181 253); display: block; font-size: 0.62rem; font-weight: 600; letter-spacing: 0.08em; margin-top: 0.4rem; text-align: center; text-transform: uppercase; }
  .comparison-axis { color: rgb(100 116 139); display: flex; font-size: 0.62rem; justify-content: space-between; }
  .comparison-group { border-top: 1px solid rgba(196, 181, 253, 0.09); margin-top: 1.15rem; padding-top: 1.15rem; }
  .comparison-row-heading { align-items: center; display: flex; gap: 1rem; justify-content: space-between; margin-bottom: 0.9rem; }
  .comparison-class { color: rgb(226 232 240); font-size: 0.86rem; }
  .comparison-delta { border: 1px solid rgba(196, 181, 253, 0.22); border-radius: 999px; color: rgb(196 181 253); font-size: 0.62rem; font-weight: 700; letter-spacing: 0.04em; padding: 0.28rem 0.55rem; }
  .comparison-delta.is-negative { color: rgb(148 163 184); }
  .comparison-delta.is-unavailable { border-color: rgba(148, 163, 184, 0.16); color: rgb(100 116 139); font-weight: 600; }
  .dumbbell-row { align-items: center; display: grid; gap: 1rem; grid-template-columns: var(--value-column) minmax(0, 1fr) var(--value-column); }
  .dumbbell-value { color: rgb(226 232 240); font-size: 0.76rem; font-variant-numeric: tabular-nums; font-weight: 700; }
  .dumbbell-value small { color: rgb(100 116 139); display: block; font-size: 0.52rem; font-weight: 600; letter-spacing: 0.08em; margin-top: 0.15rem; text-transform: uppercase; }
  .predicted-value { color: rgb(196 181 253); text-align: right; }
  .dumbbell-track { height: 1.5rem; position: relative; }
  .dumbbell-axis { background: rgba(148, 163, 184, 0.24); height: 1px; left: 0; position: absolute; right: 0; top: 50%; }
  .dumbbell-connector { background: rgb(100 116 139); height: 2px; left: var(--range-start); position: absolute; top: calc(50% - 0.5px); width: var(--range-width); }
  .dumbbell-dot { border-radius: 50%; height: 0.72rem; position: absolute; top: 50%; transform: translate(-50%, -50%); width: 0.72rem; }
  .target-dot { background: rgb(3 3 5); border: 2px solid rgb(226 232 240); left: var(--target); }
  .predicted-dot { background: rgb(139 92 246); border: 2px solid rgb(196 181 253); left: var(--predicted); }
  .dumbbell-track.is-unavailable .dumbbell-axis { background: repeating-linear-gradient(90deg, rgba(100, 116, 139, 0.3) 0 4px, transparent 4px 9px); }

  .benchmark-figure figcaption { color: rgb(148 163 184); line-height: 1.75; margin-top: 1.35rem; max-width: 66rem; }
  .benchmark-figure figcaption strong { color: rgb(196 181 253); display: block; font-size: 0.78rem; letter-spacing: 0.08em; text-transform: uppercase; }
  .benchmark-figure figcaption span { display: block; margin-top: 0.55rem; }

  .prediction-report {
    display: grid;
    gap: 1.25rem;
    grid-template-columns: minmax(0, 0.82fr) minmax(0, 1.18fr);
    margin: clamp(2rem, 4vw, 3.5rem) 0 0;
  }

  .prediction-study-copy { color: rgb(148 163 184); line-height: 1.75; }

  .prediction-report-image { background: #020203; border: 1px solid var(--case-border); border-radius: 1.1rem; overflow: hidden; padding: clamp(1rem, 3vw, 2rem); }
  .prediction-report-image img { display: block; height: auto; margin-inline: auto; max-height: 24rem; max-width: 100%; object-fit: contain; width: auto; }
  .prediction-report pre {
    background: #000;
    border: 1px solid var(--case-border);
    border-radius: 1.1rem;
    color: rgb(226 232 240);
    font-family: Consolas, "Courier New", monospace;
    font-size: clamp(0.7rem, 1.5vw, 0.88rem);
    line-height: 1.7;
    margin: 0;
    overflow-x: auto;
    padding: clamp(1.1rem, 3vw, 1.7rem);
    white-space: pre;
  }

  .prediction-report figcaption { color: rgb(100 116 139); font-size: 0.72rem; grid-column: 1 / -1; line-height: 1.55; padding: 0 0.2rem; }

  .application-section { padding-bottom: clamp(2.5rem, 4vw, 4rem); }

  .grid-case-study {
    background:
      radial-gradient(circle at 82% 9%, rgba(124, 58, 237, 0.13), transparent 25rem),
      radial-gradient(circle at 16% 48%, rgba(196, 181, 253, 0.045), transparent 28rem),
      #000;
  }

  .grid-case-study .case-hero { min-height: auto; padding-bottom: clamp(2.75rem, 4vw, 4rem); }
  .grid-case-study .case-hero-grid { min-height: auto; padding-bottom: clamp(1rem, 2vw, 2rem); padding-top: clamp(3.5rem, 6vw, 5.5rem); }
  .grid-case-study .case-section { padding-bottom: clamp(2.25rem, 3vw, 3.25rem); padding-top: clamp(2.25rem, 3vw, 3.25rem); }
  .grid-case-study .technology-section { padding-top: clamp(2.5rem, 3.5vw, 3.75rem); }

  .grid-hero-visual { margin: 0; }
  .grid-hero-visual figcaption { color: rgb(100 116 139); font-size: 0.72rem; letter-spacing: 0.04em; margin-top: 0.9rem; }
  .grid-architecture-trigger { background: #020203; border: 1px solid var(--case-border); border-radius: 1.4rem; display: block; overflow: hidden; padding: 0; width: 100%; }
  .grid-architecture-trigger img { aspect-ratio: 16 / 9; display: block; object-fit: contain; width: 100%; }

  .network-context-layout { align-items: start; display: grid; gap: clamp(1.5rem, 4vw, 4rem); grid-template-columns: minmax(18rem, 0.8fr) minmax(0, 1.2fr); margin-top: clamp(2rem, 3vw, 3rem); }
  .network-context-map { margin: 0; }
  .network-context-map button { background: #020203; border: 1px solid var(--case-border); border-radius: 1.25rem; display: block; overflow: hidden; padding: 0; width: 100%; }
  .network-context-map img { aspect-ratio: 2 / 2.35; display: block; object-fit: contain; width: 100%; }
  .network-context-map figcaption { color: rgb(100 116 139); font-size: 0.72rem; line-height: 1.55; margin-top: 0.75rem; }
  .network-context-metrics { display: grid; gap: 1rem; grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .network-context-metrics article,
  .contingency-grid article { background: var(--case-panel); border: 1px solid var(--case-border); border-radius: 1.1rem; min-height: 12rem; padding: clamp(1.35rem, 2.5vw, 1.85rem); }
  .network-context-metrics span,
  .contingency-grid span { color: rgb(167 139 250); display: block; font-size: 0.72rem; font-weight: 700; letter-spacing: 0.14em; }
  .network-context-metrics strong { color: rgb(226 232 240); display: block; font-size: clamp(1.7rem, 3vw, 2.6rem); letter-spacing: -0.04em; margin-top: 0.75rem; }
  .network-context-metrics p,
  .contingency-grid p { color: rgb(148 163 184); line-height: 1.7; margin-top: 0.65rem; }
  .network-context-metrics .source-note { grid-column: 1 / -1; margin: 0.25rem 0 0; }
  .contingency-grid { display: grid; gap: 1rem; grid-template-columns: repeat(3, minmax(0, 1fr)); margin-top: clamp(2rem, 3vw, 3rem); }
  .contingency-grid strong { color: rgb(226 232 240); display: block; font-size: 1.05rem; margin-top: 1rem; }
  .concept-note { color: rgb(100 116 139); font-size: 0.75rem; line-height: 1.6; margin-top: 1rem; }

  .literature-link { border-radius: 999px; display: inline-flex; margin-top: 1.75rem; min-height: 2.75rem; padding: 0.62rem 1.15rem; width: fit-content; }

  .grid-pipeline li { min-height: 10.5rem; }
  .grid-reflection { margin-top: clamp(2rem, 3vw, 3rem); }

  .crof-heading { max-width: 58rem; }
  .crof-heading > p:last-child { max-width: 54rem; }
  .crof-gallery { display: grid; gap: clamp(1.25rem, 2.5vw, 2rem); grid-template-columns: repeat(2, minmax(0, 1fr)); margin-top: clamp(2rem, 3vw, 3rem); }
  .crof-gallery figure { margin: 0; min-width: 0; }
  .crof-gallery button { aspect-ratio: 16 / 10; background: rgb(5 5 8); border: 1px solid var(--case-border); border-radius: 1.2rem; display: block; overflow: hidden; padding: 0; width: 100%; }
  .crof-gallery img { display: block; height: 100%; object-fit: contain; width: 100%; }
  .crof-gallery figcaption { color: rgb(100 116 139); font-size: 0.72rem; line-height: 1.55; margin-top: 0.75rem; }

  .renewable-case-study {
    background:
      radial-gradient(circle at 86% 8%, rgba(124, 58, 237, 0.12), transparent 27rem),
      radial-gradient(circle at 12% 54%, rgba(196, 181, 253, 0.04), transparent 30rem),
      #000;
  }
  .renewable-case-study .case-hero { min-height: auto; padding-bottom: clamp(2.75rem, 4vw, 4rem); }
  .renewable-case-study .case-hero-grid { min-height: auto; padding-bottom: clamp(1rem, 2vw, 2rem); padding-top: clamp(3.5rem, 6vw, 5.5rem); }
  .renewable-case-study .case-section { padding-bottom: clamp(2.4rem, 3.4vw, 3.7rem); padding-top: clamp(2.4rem, 3.4vw, 3.7rem); }
  .renewable-hero-visual,
  .renewable-control-visual { margin: 0; }
  .renewable-hero-visual button,
  .renewable-control-visual button { background: rgb(4 4 7); border: 1px solid var(--case-border); border-radius: 1.35rem; display: block; overflow: hidden; padding: 0; width: 100%; }
  .renewable-hero-visual img,
  .renewable-control-visual img { aspect-ratio: 16 / 9; display: block; object-fit: cover; width: 100%; }
  .renewable-hero-visual figcaption,
  .renewable-control-visual figcaption { color: rgb(100 116 139); font-size: 0.72rem; line-height: 1.55; margin-top: 0.8rem; }

  .plant-flow { display: grid; gap: 4.6rem; list-style: none; margin: clamp(2.5rem, 5vw, 4.5rem) auto 0; max-width: 48rem; padding: 0; }
  .plant-flow li { align-items: center; display: grid; gap: clamp(1.4rem, 4vw, 3rem); grid-template-columns: 5rem minmax(0, 1fr); position: relative; }
  .plant-flow li:not(:last-child)::before { background: linear-gradient(180deg, rgb(196 181 253), rgba(139, 92, 246, 0.28)); content: ""; height: 3.35rem; left: 2.48rem; position: absolute; top: calc(100% + 0.65rem); width: 1px; }
  .plant-flow li:not(:last-child)::after { border-bottom: 1px solid rgb(196 181 253); border-right: 1px solid rgb(196 181 253); content: ""; height: 0.42rem; left: 2.28rem; position: absolute; top: calc(100% + 3.55rem); transform: rotate(45deg); width: 0.42rem; }
  .plant-flow-symbol { align-items: center; background: radial-gradient(circle, rgba(124, 58, 237, 0.14), transparent 70%); border: 1px solid rgba(196, 181, 253, 0.38); border-radius: 50%; color: rgb(196 181 253); display: flex; height: 5rem; justify-content: center; position: relative; width: 5rem; }
  .plant-flow-symbol::before { animation: plant-symbol-orbit 11s linear infinite; background: conic-gradient(from 0deg, transparent 0 66%, rgba(167, 139, 250, 0.28) 76%, rgba(255, 255, 255, 0.72) 86%, rgba(196, 181, 253, 0.4) 94%, transparent 100%); border-radius: inherit; content: ""; filter: drop-shadow(0 0 0.32rem rgba(167, 139, 250, 0.34)); inset: -0.18rem; mask: radial-gradient(farthest-side, transparent calc(100% - 1px), #000 calc(100% - 1px)); pointer-events: none; position: absolute; }
  .plant-flow-symbol svg { height: 2rem; position: relative; width: 2rem; z-index: 1; }
  @keyframes plant-symbol-orbit { to { transform: rotate(360deg); } }
  .plant-flow li div > span { color: rgb(167 139 250); display: block; font-size: 0.66rem; font-weight: 700; letter-spacing: 0.16em; }
  .plant-flow strong { color: rgb(226 232 240); display: block; font-size: clamp(1.05rem, 2vw, 1.3rem); line-height: 1.35; margin-top: 0.55rem; }
  .plant-flow p { color: rgb(148 163 184); line-height: 1.7; margin-top: 0.45rem; }

  .renewable-control-visual { margin: clamp(2rem, 4vw, 3.5rem) auto 0; max-width: 68rem; }
  .renewable-control-visual button { max-height: 38rem; }
  .renewable-control-visual picture { display: block; }
  .renewable-control-visual img { max-height: 38rem; object-fit: contain; object-position: center; }
  .renewable-case-study .technology-section { padding-top: clamp(2.5rem, 3.5vw, 3.75rem); }

  .responsible-use-section { padding-bottom: clamp(2.5rem, 4vw, 4rem); padding-top: clamp(1rem, 2vw, 2rem); }
  .technology-section { padding-top: clamp(3.5rem, 6vw, 6rem); }
  .report-access-control { border-radius: 999px; display: inline-flex; margin-top: clamp(2rem, 4vw, 3rem); min-height: 2.75rem; padding: 0.62rem 1.15rem; width: fit-content; }

  .case-footer { align-items: center; display: flex; justify-content: center; padding-bottom: 6rem; padding-top: 4rem; position: relative; }
  .case-footer::before { animation: footer-neon-line 7s linear infinite; background: linear-gradient(90deg, transparent 0%, rgba(139, 92, 246, 0.42) 14%, rgb(196 181 253) 38%, rgba(255, 255, 255, 0.92) 50%, rgb(196 181 253) 62%, rgba(139, 92, 246, 0.42) 86%, transparent 100%); background-size: 220% 100%; content: ""; height: 1px; left: clamp(1.35rem, 5vw, 5rem); position: absolute; right: clamp(1.35rem, 5vw, 5rem); top: 0; }
  @keyframes footer-neon-line {
    from { background-position: 120% 0; }
    to { background-position: -120% 0; }
  }
  .research-references { max-width: 36rem; text-align: center; }
  .research-references > span { color: rgb(139 92 246); display: block; font-size: 0.65rem; font-weight: 700; letter-spacing: 0.14em; }
  .research-references a { align-items: center; color: rgb(148 163 184); display: inline-flex; flex-direction: column; font-size: 0.78rem; gap: 0.75rem; line-height: 1.6; margin-top: 0.75rem; text-decoration: none; }
  .research-references img { height: 1.75rem; object-fit: contain; width: auto; }
  .research-references a span { text-decoration: underline; text-decoration-color: rgba(196, 181, 253, 0.35); text-underline-offset: 0.22em; }
  .research-references a:hover,
  .research-references a:focus-visible { color: rgb(196 181 253); }
  .research-references a:hover img,
  .research-references a:focus-visible img { filter: drop-shadow(0 0 0.5rem rgba(167, 139, 250, 0.5)); }

  .tech-scope-item {
    border: 1px solid rgba(221, 214, 254, 0.3);
    border-radius: 0.7rem;
    background: rgba(167, 139, 250, 0.1);
    color: rgb(221 214 254);
    cursor: default;
    font-size: 0.875rem;
    font-weight: 600;
    padding: 0.55rem 0.8rem;
  }

  @media (max-width: 900px) {
    .case-hero-grid,
    .split-copy { grid-template-columns: 1fr; }
    .case-hero-grid { min-height: auto; padding-top: 5rem; }
    .hero-visual { max-width: 38rem; }
    .dataset-sources { grid-template-columns: 1fr; }
    .prediction-report { grid-template-columns: 1fr; margin-top: 2rem; }
    .network-context-layout,
    .network-context-metrics,
    .contingency-grid,
    .crof-gallery { grid-template-columns: 1fr; }
    .network-context-metrics article,
    .contingency-grid article { min-height: auto; }
    .network-context-metrics .source-note { grid-column: auto; }
    .grid-case-study .case-hero-grid { padding-bottom: 0.5rem; padding-top: 5rem; }
    .grid-case-study .case-section { padding-bottom: 2.25rem; padding-top: 2.25rem; }
    .renewable-case-study .case-hero-grid { padding-bottom: 0.5rem; padding-top: 5rem; }
    .renewable-case-study .case-section { padding-bottom: 2.25rem; padding-top: 2.25rem; }
    .class-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .research-references { justify-self: center; text-align: center; }
  }

  @media (max-width: 48rem) {
    .source-links a.dataset-source-card:hover .dataset-source-arrow,
    .source-links a.dataset-source-card:focus-visible .dataset-source-arrow,
    .source-links a.dataset-source-card:active .dataset-source-arrow {
      transform: translateY(-50%) scaleX(1.08);
      transform-origin: right center;
    }
  }

  @media (max-width: 640px) {
    .case-hero { min-height: auto; padding-top: 1.5rem; }
    .case-hero-grid { padding-top: 4rem; }
    .case-hero h1,
    .section-heading h2,
    .split-copy h2 { font-size: clamp(2rem, 9vw, 2.75rem); }
    .metric-grid,
    .class-grid { grid-template-columns: 1fr; }
    .plant-flow { gap: 4rem; }
    .plant-flow li { gap: 1.15rem; grid-template-columns: 3.75rem minmax(0, 1fr); }
    .plant-flow-symbol { height: 3.75rem; width: 3.75rem; }
    .plant-flow-symbol svg { height: 1.55rem; width: 1.55rem; }
    .plant-flow li:not(:last-child)::before { height: 2.85rem; left: 1.85rem; top: calc(100% + 0.55rem); }
    .plant-flow li:not(:last-child)::after { left: 1.65rem; top: calc(100% + 2.95rem); }
    .renewable-control-visual { max-width: 22rem; }
    .renewable-control-visual button,
    .renewable-control-visual img { max-height: none; }
    .renewable-control-visual img { aspect-ratio: 2 / 3; }
    .metric-grid article { min-height: 7.5rem; }
    .dataset-bar { gap: 1px; grid-template-columns: 7fr 2fr 1fr; }
    .dataset-bar > div { min-height: 3.85rem; min-width: 0; overflow: hidden; padding: 0.55rem 0.28rem; }
    .dataset-bar > div:first-child { border-radius: 1rem 0 0 1rem; }
    .dataset-bar > div:last-child { border-radius: 0 1rem 1rem 0; }
    .dataset-bar strong { font-size: clamp(0.72rem, 4.6vw, 1.3rem); letter-spacing: -0.035em; }
    .dataset-bar span { font-size: 0; margin-top: 0.45rem; }
    .dataset-bar .train span::after,
    .dataset-bar .validation span::after,
    .dataset-bar .test span::after { font-size: 0.58rem; letter-spacing: 0; }
    .dataset-bar .train span::after { content: "70%"; }
    .dataset-bar .validation span::after { content: "20%"; }
    .dataset-bar .test span::after { content: "10%"; }
    .performance-card { padding: 1.2rem; }
    .comparison-plot { --value-column: 3.85rem; }
    .comparison-scale { margin-left: var(--value-column); }
    .comparison-row-heading { align-items: flex-start; }
    .comparison-delta { font-size: 0.56rem; white-space: nowrap; }
    .dumbbell-row { gap: 0.55rem; }
    .dumbbell-value { font-size: 0.67rem; }
    .dumbbell-value small { font-size: 0.46rem; }
    .dumbbell-dot { height: 0.65rem; width: 0.65rem; }
    .research-figure { border-radius: 0.9rem; margin-top: 2rem; }
    .figure-zoom-trigger > span { opacity: 1; }
    .figure-lightbox { padding: 0.8rem; }
    .figure-lightbox-content > img { max-height: calc(100dvh - 2rem); }
    .figure-lightbox-close { right: 0.35rem; top: 0.35rem; }
    .report-viewer { padding: 0.45rem; }
    .report-viewer-content { border-radius: 0.75rem; }
    .report-viewer-close { right: 0.45rem; top: 0.45rem; }
    .scan-line { display: none; }
  }

  @media (prefers-reduced-motion: reduce) {
    .scan-line,
    .case-footer::before,
    .plant-flow-symbol::before { animation: none; }
  }
</style>
