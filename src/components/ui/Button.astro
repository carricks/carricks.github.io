---
interface Props {
  href?: string;
  variant?: "primary" | "secondary";
}

const {
  href = "#",
  variant = "primary",
} = Astro.props;

const variants = {
  primary:
    "border border-violet-200/30 bg-violet-300 text-zinc-900",

  secondary:
    "border border-white/10 hover:border-cyan-400 hover:text-cyan-400",
};
---

<a
  href={href}
  class={`
inline-flex
items-center
justify-center
rounded-xl
px-6
py-3
font-medium
transition-all
duration-300
interactive-control
${variants[variant]}
`}
>
  <slot />
</a>
