---
interface Props {
  id?: string;
  class?: string;
}

const {
  id = "",
  class: className = "",
} = Astro.props;
---

<section
  id={id}
  class={`py-24 lg:py-32 ${className}`}
>
  <slot />
</section>